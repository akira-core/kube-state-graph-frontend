## 0. Branch and plan (done)

- [x] 0.1 Create `feat/network-trace` and commit the integration plan as `openspec/changes/network-trace-sankey/plan.md` in its own commit before any implementation; verify `git log --oneline -1` shows `docs(network-trace): add integration plan for sankey-panel port`

## 1. Spec skeleton

- [x] 1.1 Write `proposal.md`, `design.md` (D1–D9 and the `validate.ts` mapping table), `tasks.md` and the delta specs under `specs/` for `network-trace`, `sankey-canvas`, `app-shell`, `runtime-config`, `graph-data-source`, `graph-view`, `storage-flow-sankey` and `dev-environment`; verify `openspec validate network-trace-sankey --strict` passes and `npx prettier --check openspec/changes/network-trace-sankey` is clean

## 2. Extract the shared `sankey-canvas` module (design D8)

- [x] 2.1 Move `useZoomPan.ts` (with `openingViewport`, `fitViewport`, `Viewport`, `Size`, `ZoomPanApi`) and its test into `src/features/sankey-canvas/` and repoint the storage imports; verify `src/features/sankey-canvas/useZoomPan.test.ts` passes unchanged apart from its path
- [x] 2.2 Move `SankeyControlBar.tsx` and extract `geometry.ts` (`CARD_W`, `LEAF_W`, `HEADER_H`, `BODY_MIN`, `ROW_MIN_H`, `ROW_GAP`, `MIN_THICKNESS`, `MAX_THICKNESS`, `LABEL_MIN_THICKNESS`, `PAD_TOP`, `stackHeight`, `placeStack`, `ribbonPath`, `thicknessScale`, the namespace palette function) out of `layoutSankey.ts`, which now imports them; verify `src/features/storage-flow-sankey/layoutSankey.test.ts` and `sankeyPerformance.test.ts` pass with no expectation changed
- [ ] 2.3 Extract `SankeyCanvas.tsx` (host div, svg without `viewBox`, transform group, column header `<text>`; props `{ columns, viewport, hostRef, hostProps, dragging, onKeyDown, children, defs? }`) and `SankeyCard.tsx` / `SankeyWrapperBox.tsx` (props: position and size, `label`, `subtitle`, `kind`, `status`, `dashed`, `locatable`, `faded`, `namespaceColor`, `extraLines?`, `slotLabels?`, events) from `SankeyChart.tsx`, and recompose `SankeyChart` from them; verify `src/features/sankey-canvas/SankeyCard.test.tsx` renders the storage call sites to the same SVG markup as before the move (snapshot taken on the pre-move tree) and `SankeyView.test.tsx` passes unchanged
- [ ] 2.4 Extract `useSankeyTooltip.ts` + `SankeyTooltip.tsx` (tip state, `useLayoutEffect` clamping, overlay div), `useSankeyKeyboard.ts` (the `+ - 0 1 F Esc` handler that ignores `input` / `select` / `textarea` / radio targets), `useContainerSize.ts` (`ResizeObserver` plus first measurement), `useOpeningViewport.ts` (one-time fit) and `StatusLegend.tsx` from `SankeyView.tsx`, which now composes them; verify `src/features/sankey-canvas/useSankeyKeyboard.test.ts` covers every key and the input exclusion, and `SankeyView.test.tsx` passes unchanged
- [x] 2.5 Publish everything through `src/features/sankey-canvas/index.ts`, drop the corresponding re-exports from `storage-flow-sankey/index.ts`, and run the single-entry audit; verify `grep -rn "useZoomPan\|ribbonPath\|stackHeight\|placeStack\|handleKeyDown\|ResizeObserver" src/features` matches only `src/features/sankey-canvas/`, and `npm run test:ci` plus `npm run e2e` (`tests/sankey-svm-grouping.spec.ts`, `tests/storage-graph.spec.ts`) are green before phase 5 starts

## 3. Shared foundations (design D1, D6)

- [x] 3.1 Add `'host'` to `NodeKind` and `'network-flow'` to `EdgeType` in `src/shared/constants/types.ts`, then fill every registry the compiler flags: `iconSvgByKind.ts` (host glyph), `categoryByKind.ts` (`host: 'Other'`), `colorByEdgeType.ts` (`EDGE_ENDPOINTS_BY_TYPE['network-flow'] = { from: 'switch', to: 'host' }`, `EDGE_IS_TRAFFIC_BY_TYPE['network-flow'] = false`, `EDGE_STYLE_BY_TYPE['network-flow'] = { color: tokens.edge['network-flow'], solid, routing: 'taxi' }`), `drawnEdgeTypesForMode.ts` (both modes); verify the `categoryByKind` parity test and `src/features/graph-view/GraphView.test.tsx` pass, and the regenerated `getStylesheet` snapshot diff contains exactly the two new entries
- [x] 3.2 Add the theme tokens `kind.host`, `edge['network-flow']`, `sankey.traceFlow` / `traceFlowEnd`, `sankey.traceBackward` / `traceBackwardEnd`, `sankey.traceResidualIn` / `traceResidualOut` to both palettes in `src/shared/theme/tokens.ts`; verify `src/shared/theme/tokens.test.ts` parity passes
- [x] 3.3 Widen the types: `EdgeFlowMetrics { deltaBps?: number }`, `EdgeMetrics = EdgeRedMetrics | (EdgeIoMetrics & EdgeFlowMetrics)`, `NodeDataDefinition.investigation? / clients? / otherInBps? / otherOutBps?` in `src/shared/types/cytoscape.d.ts`; `WireFlowMetrics`, `WireInvestigation`, `WireClient`, the node fields and `name?` in `src/shared/types/wire.ts`; verify `npm run typecheck` passes with no storage file edited
- [x] 3.4 Extend `src/features/graph-data/normalize.ts`: `parseIoMetrics` reads `delta_bps` (finite, ≥ 0) into `deltaBps`; `parseNodes` gains `parseInvestigation`, `parseClients` and `parseNonNegativeBps`, each dropping the malformed field and pushing an `errors` entry; verify `src/features/graph-data/normalize.test.ts` covers every row of the D5 table that lands in normalize, and a new byte-identical regression asserts `normalizeGraph(SHOWCASE_GRAPH)` and `normalizeGraph(SHOWCASE_STORAGE_GRAPH)` deep-equal their pre-change output
- [x] 3.5 Add `formatBitsPerSec` and `formatDeltaBps` to `src/shared/format/measurements.ts` on the existing significant-digit ladder (`bps` / `kbps` / `Mbps` / `Gbps` / `Tbps`); verify `src/shared/format/measurements.test.ts` covers `10e9 → 10 Gbps`, `+10 Gbps`, `0 → 0 bps`, a sub-unit value in exponential form, and that only `formatDeltaBps` emits the `+`
- [x] 3.6 Add the `Δ rate` row to `HoverTooltip.tsx` when an edge carries `deltaBps`, and the `investigation` / `clients` summary rows to `src/shared/nodeAttributes/buildNodeAttributes.ts`; verify `HoverTooltip` and `buildNodeAttributes` tests cover presence and absence of each field

## 4. Configuration and request (design D3)

- [x] 4.1 Add `trace?` to `RuntimeEndpoints` in `src/features/runtime-config/types.ts` and `'trace'` to `KNOWN_ENDPOINT_KEYS` and the parse loop in `validate.ts`; verify `src/features/runtime-config/validate.test.ts` accepts a valid `endpoints.trace`, treats `""` as absent, rejects a malformed value naming the key, and passes a document without it
- [x] 4.2 Add `src/features/graph-data/traceRequestUrl.ts` (`TraceQuery`, `TRACE_DEFAULTS`, `buildTraceRequestUrl(endpoint, range, query, nowMs?)`: seconds × 1000, all seven parameters explicit, empty hostname → `undefined`, built with `withQuery`) and export it from `graph-data/index.ts`; verify `src/features/graph-data/traceRequestUrl.test.ts` asserts 13-digit `from_ts` / `to_ts`, the seven parameters with defaults spelled out, re-resolution of a relative window at send time, and `undefined` for an empty hostname
- [x] 4.3 Add `src/features/network-trace/traceUrlScope.ts` (`TraceDraft`, `TraceUrlScope`, `TRACE_DEFAULTS`, `parseTraceScope`, `serializeTraceScope`, `buildTraceQuery`, `cleanMinBps`) per the D3 rules; verify `src/features/network-trace/traceUrlScope.test.ts` covers missing = default, invalid raw kept in the draft with a problem, defaults omitted on serialize, `track_dir=destination` written, `min_bps` written only when > 0, `cleanMinBps` flooring, and `buildTraceQuery` refusing each invalid field with a message

## 5. Model port (design D4, D5, D7)

- [ ] 5.1 Port `model/types.ts`, `classify.ts` (`HOP_KINDS`, `GROUP_KINDS`, `FLOW_EDGE_TYPES = ['network-flow']`, `AUTO_TIER`, `classOf`, `recKind`, `worstStatus`, `weightOf` reading `deltaBps` only, `clientsOf`, `infoOf`, `isPlacementEdge`), `nodeIndex.ts`, `util.ts` (`SEP`, `sum`, `mustGet`); verify `src/features/network-trace/model/classify.test.ts` covers the three kind classes, the auto-tier kinds, placement-edge detection by both spellings, and that `storage-flow` is not a flow edge
- [x] 5.2 Port `investigation.ts` (`resolveInvestigation`: at most one, hop kind only; `addAnchor`), `validate.ts` (`validateTraceSemantics`) and `deriveTrace.ts` (`deriveTrace`, `resolveTraceDirection`, no mutation of `elements`); verify `src/features/network-trace/model/deriveTrace.test.ts` returns `ok: false` with the listed message for two investigations, a group endpoint, a leaf continuing onward, a colliding synthesized id and a body with no hop, returns a warning when `investigation.direction` disagrees with `track_dir`, produces no anchor when no node carries `investigation`, does not crash on a storage-only payload, and leaves the input deep-equal
- [x] 5.3 Port `scan.ts`, `edges.ts` (same-key aggregation, threshold filtering with the anchor edge and derived edges exempt), `prune.ts`, `residuals.ts` (balance equation, explicit `otherInBps` / `otherOutBps`, filtered amounts folded in, source exemption, `eps = max(in, out) × 0.005 + 1`), `clusters.ts`, `assemble.ts`; verify `deriveTrace.test.ts` asserts on every sample that each hop satisfies `tracedIn + otherIn ≈ tracedOut + otherOut` within eps, that a source hop gets no other-in residual, that a residual below eps is not emitted, that an explicit unbalanced pair is drawn as given with a warning, and that a `minBps` of `5e8` hides ribbons and folds their sum into the residuals while the anchor edge survives
- [ ] 5.4 Port `columns.ts` steps 5a–5g (tier super-nodes, majority-direction cycle breaking, SCC, longest path, backward / lateral marking, sub-order); verify `src/features/network-trace/model/columns.test.ts` covers tier locking, the two-way and three-way cycle cases with their warnings, and the start hop pinned to the correct side for both directions
- [x] 5.5 Port owner aggregation and the derived pod → application → namespace cards; verify `deriveTrace.test.ts` covers a single-owner port producing a metered ribbon, a mixed port (another owner or an unknown owner) producing ownership lines only, a port with no known owner producing no owner layer, the owner card's total suffixes, a leaf pod with an application ancestor, one with only a namespace ancestor, one with only `labels.namespace`, one with none, and a proxy pod producing no derived link
- [ ] 5.6 Port `hoverPath.ts`, `locatable.ts`, `aggregates.ts`, `summary.ts` (structured rows) and convert `sankey-panel`'s `samples/*.json` (except `storage.json`) into `src/features/network-trace/testing/samples.ts`; verify `src/features/network-trace/model/hoverPath.test.ts` (upstream ∪ downstream through derived and ownership edges, wrapper = union of members), `locatable.test.ts` and `aggregates.test.ts` pass
- [x] 5.7 Add `src/features/network-trace/invariants.test.ts`: no hex colour, no CSS file, no `className` string style and none of `leaf-stop` / `n-title` / `zoom-layer` / `chart-focus` under `src/features/network-trace/`, and the literal `'+'` appears only in `formatDeltaBps`; verify it passes

## 6. Layout port

- [ ] 6.1 Port `layout/constants.ts` (only `RES_LEN`, `RES_GAP`, `CLIENT_ROW_H`, `CLIENT_PAD`, `CLIENT_GAP`, `OWN_T`), `layout/geometry.ts` (`Slot` with residual slots and `role`, `NodeGeom`, `EdgeGeom` with `bulge` / `backY`, `WrapperGeom`, `Geometry`), `layout/text.ts` (card line lists, `clientCols` / `clientRows`, `clip`, heights from `ROW_MIN_H`) and `layout/paths.ts` (`ownLine`, `lateralRibbon`, `backwardRibbon`; ordinary ribbons use the shared `ribbonPath`); verify `src/features/network-trace/layout/text.test.ts` covers the clients table column omission and truncation
- [x] 6.2 Port `layoutTrace.ts` (`layoutTrace(model, { order })`, `DEFAULT_ORDER = 'flow'`, barycenter retained, residuals as real slots on the shared thickness scale, geometry constants from `sankey-canvas/geometry.ts`); verify `src/features/network-trace/layout/layoutTrace.test.ts` carries the ported `flow-order` and `entry` cases (flow ordering, namespace adjacency, lateral chain adjacency with producer above, tie fallback to barycenter, determinism)
- [x] 6.3 Port `tooltips.ts` (`bandTooltipLines`, `nodeTooltipLines`, `residualTooltipLines`, `colCaption`, `BandMeta` rendered through an exhaustive `Record<keyof BandMeta, …>`); verify `src/features/network-trace/layout/tooltips.test.ts` covers each optional line's presence and absence and the column captions
- [x] 6.4 Add `src/features/network-trace/golden.test.ts`: every sample × `minBps` in `{ 0, 5e8 }` × `layout: 'node'` × `order: 'barycenter'`, deep-frozen input, model plus geometry to three decimals; verify the snapshot is committed after a visual check of the fixture and the suite passes

## 7. Chart and view

- [ ] 7.1 Implement `chart/TraceDefs.tsx` (two gradients `ksg-trace-grad-flow`, `ksg-trace-grad-back`), `chart/TraceCards.tsx` (every card a `SankeyCard` / `SankeyWrapperBox`: hop boxes with `slotLabels`, leaf cards with the clients table in `extraLines` and the role text, pod / application / namespace / owner cards, the anchor card, the `Residual` blocks; `data-testid="trace-node-<label>"`, `data-kind`, `data-status`, `data-locatable`) and `chart/TraceChart.tsx` composing `SankeyCanvas` with ribbons → value labels → wrappers → cards → residuals; verify `src/features/network-trace/chart/TraceCards.test.tsx` (ported `cards` test: every `<text y>` inside its card frame via `renderToStaticMarkup`) and `TraceChart.test.tsx` (z-order, testids, `lit` fading) pass
- [x] 7.2 Implement `TraceView.tsx` with the shared hooks and components only (`useContainerSize`, `useOpeningViewport`, `useZoomPan`, `useSankeyKeyboard`, `useSankeyTooltip` + `SankeyTooltip`, `SankeyControlBar`, `StatusLegend`), the control bar (`Layout` Flat|Node, `Min Δ` raw string with 200 ms debounce and blur normalisation, Clear, the hidden pill, `Order` Flow|Barycenter), the six empty states `trace-empty-unconfigured|scope|awaiting|cancelled|model-error|filtered` and the warnings drawer; verify `src/features/network-trace/TraceView.test.tsx` covers each empty state, the debounce, the pill text, the layout and order switches preserving the viewport, the keyboard shortcuts, and the tooltip clearing when the hovered node disappears
- [ ] 7.3 Implement `TraceScopeBar.tsx` (`ScopeSelect` hostname with candidates and custom value, Max hops / Top N / Threshold % inputs, Track select, `QueryButton` with the first problem as `disabledReason`, problems under `data-testid="trace-scope-problem"`), `TraceLegend.tsx` (rows only for what the chart contains) and `TraceSummary.tsx` (hop balance table and namespace subtotals, `dataTableClass`); verify `src/features/network-trace/TraceScopeBar.test.tsx` covers the disabled reasons and that editing issues no request, `TraceLegend.test.tsx` the presence-gated rows, and `TraceSummary.test.tsx` the balance rows
- [x] 7.4 Add `useHostnameCandidates.ts` (`switchHostnames(elements)` plus a memo hook); verify `src/features/network-trace/useHostnameCandidates.test.ts` lists only `switch` kind labels, deduplicated and sorted, and an empty list before the first payload
- [x] 7.5 Add `src/shared/fixtures/showcaseTrace.ts` (`SHOWCASE_TRACE`: the network samples of sankey-panel merged into one destination trace — `dci-uturn` backbone and start, `k8s` / `client` stitched under its ToRs, the other samples beside it, ids prefixed by sample key), a third target in `dev/buildFixture.mjs`, `public/demo/trace.json` and the `.prettierignore` entry; verify `src/shared/fixtures/showcaseTrace.test.ts` asserts empty `errors`, every referenced id present, one investigation, every merged sample present, at least one of each: lateral, backward, clients, owner, derived namespace, k8s node hop, residuals, and `npm run fixture:build && npm run fixture:check` is a no-op afterwards

## 8. Shell integration (design D2)

- [x] 8.1 Rework `NavBar.tsx` into `CATEGORIES` (Storage → `/graph`, Network → `/network/graph`, bare paths, `aria-label="Category"`) and `VIEWS_BY_CATEGORY` (`aria-label="View"`, Network view links carrying `location.search`), category decided by `pathname.startsWith('/network')`; verify `src/features/app-shell/NavBar.test.tsx` asserts one Graph / Sankey link pair at a time, the active category and view, and that the Network view link preserves the query
- [x] 8.2 Add the routes in `AppShell.tsx` (`network` → `NetworkRedirect` to `/network/graph` with replace keeping the search; `network/:view` → `NetworkPage`), export `NotFoundPage`, and extend `AppLayout` (`isNetwork*` flags, tab title `— Network Graph` / `— Network Sankey`, focus-mode reset, `notFound`, refresh interval); verify `src/features/app-shell/AppShell.test.tsx` covers the redirect keeping the query, the titles, and `/network/foo` rendering the not-found page with the nav bar
- [x] 8.3 Implement `NetworkPage.tsx`: one `useGraphLoader`, `useAppliedScope` + `useDraft` + `useSeedTimeOnMount`, `onQuery` (refuse with problems or commit → persist → arm → `run`), `onMinBps` (component state in demo, `commit` otherwise), the `setStatus` effect with `reloadDisabled`, unmount cleanup, Locate via `navigate({ pathname: '/network/graph', search }, { state: { locate } })` and its consumption by `GraphView`, and the `view` switch between `GraphView` and `TraceView`; verify `AppShell.test.tsx` asserts 0 requests on mount, exactly one request with the seven parameters on Query, 0 requests and retained `hasPayload` on a view switch, invalid URL values shown as problems with Query disabled, Reload disabled before the first commit and when `endpoints.trace` is absent, and the demo fixture rendering on mount

## 9. End-to-end coverage and documentation

- [x] 9.1 Add `tests/network-trace.spec.ts` (modelled on `tests/explicit-query.spec.ts`): stub `config.json` with `endpoints.trace: '/demo/trace.json'`; `/network/sankey?hostname=sw-tor-1&from=now-1h&to=now` shows `trace-empty-awaiting` with 0 requests and Reload disabled; Query issues one request containing `hostname=sw-tor-1&max_hops=7&top_n=3&threshold=10&track_dir=source` and 13-digit `from_ts` / `to_ts`; `trace-svg` is visible; switching to Graph shows `graph-canvas` with the request count unchanged and `hostname` kept in the URL; back on Sankey a `Min Δ` shows the pill with no request; Cancel keeps the previous drawing; verify it passes under `npm run e2e` (the shared canvas exposes `sankey-svg`, not `trace-svg`; the bundled fixture draws under `track_dir=destination`, so the drawing cases deep-link with it and a separate case asserts the bare defaults)
- [x] 9.2 Extend `tests/demo.spec.ts`: `/network/sankey` in demo mode renders the fixture and the legend rows; verify it passes (covered by the demo-mode case in `tests/network-trace.spec.ts`)
- [x] 9.3 Update `README.md` (three demo fixtures, `endpoints.trace` with the `/api/v1/trace` example, the Network category architecture, the `/network/*` URL parameter table, troubleshooting), `deploy/README.md` and `deploy/configmap.yaml` (`trace`); verify the sections read against the delta specs' wording and `kubectl apply -k deploy/ --dry-run=client` succeeds
- [ ] 9.4 Run `npm run typecheck && npm run lint && npm run test:ci && npm run fixture:check && npm run e2e`, the single-entry audit of 2.5, and `openspec validate network-trace-sankey --strict`; verify all are green and record counts in the PR description
- [ ] 9.5 Connect to a real backend (`dev/config.local.json` with `endpoints.trace: "/api/v1/trace"` and `KSG_DEV_PROXY_TARGET`) and confirm the request Query sends carries the seven parameters and millisecond timestamps; verify by the browser network panel and record the URL in the PR description
