## 1. Routes and shell (design D1, D2, D9; spec `app-shell`, `graph-view`)

- [x] 1.1 Rewrite `src/features/app-shell/routes.ts` as the three-row `ROUTES` (`path`, `title`, `kind`) plus the two aliases (`/` → `/graph`, `/network` → `/network/sankey`), dropping `Category`, `View`, `keepSearch`, `categoryOf`, `categoryHome`, `viewsOf`, `CATEGORY_LABEL` and `VIEW_LABEL`; verify `routes.test.ts` covers `isKnownPath` for the three routes and two aliases, `/network/graph` as unknown, and the titles `Graph` / `Sankey` / `Network Sankey`
- [x] 1.2 In `AppShell.tsx` mount one `<Route>` per row and one search-keeping replace redirect per alias, derive `isAnySankey` from `kind`, and reduce `NetworkPage.tsx` to the Sankey body (no `useParams`, no `routeFor` guard, no `GraphView`, no `useNavigate` / `useLocation` / `useLocateFromNavigation`); verify `AppShell.test.tsx` covers "`/network` redirects and keeps the query", "Unknown Network view shows the not-found page" (`/network/graph` with `hostname` issues no request, keeps the nav), "Tab titles name the Network views", and that the Storage locate tests still pass unchanged
- [x] 1.3 Remove the `Category` and `View` groups from `NavBar.tsx` together with its router imports; verify `NavBar.test.tsx` asserts the navigation landmark contains no link on `/graph`, `/sankey` and `/network/sankey` while the time range, status readout, Reload, theme control and demo badge still render ("The nav bar links nowhere", "The current view's link is presented as active")
- [x] 1.4 Remove `GraphView`'s `unconfiguredMessage` prop, `describeGraphOutcome`'s `unconfigured` outcome and `NOTICE_TEST_ID.unconfigured`; keep `TRACE_UNCONFIGURED_MESSAGE` local to `TraceView.tsx` reading `Trace endpoint is not configured. The Storage pages are unaffected.` and drop it from the barrel; verify `GraphView.test.tsx`, `describeGraphOutcome.test.ts` and `AppShell.test.tsx` no longer reference `graph-unconfigured` and `npm run typecheck` passes

## 2. The scope bar's trailing slot (design D4; spec `app-shell` "Top nav bar")

- [x] 2.1 Add `trailing?: ReactNode` to `SankeyScopeBar` and `TraceScopeBar`, rendered after `QueryButton` inside the existing flex-wrap row behind a hairline, right-aligned when the row has room; verify a unit test for each bar renders the slot's content after the Query button inside the same `sankey-controls` / `trace-controls` element, and inside it when `hideQuery` is set
- [x] 2.2 Create `src/features/storage-flow-sankey/SankeyViewControls.tsx` rendering the mode `Segmented`, `Layout`, `SVM` with its disabled reason, the cut statement (`sankey-top-pods-label`, `<shown> of <total> pods`, absent when nothing is cut), `StatusLegend` and the read / write swatches gated by mode, with today's test ids; verify `SankeyViewControls.test.tsx` covers "Group is unavailable when no claim aggregate is reported", the cut statement for `10 of 15 pods` and its absence, and the legend rows per mode
- [x] 2.3 Create `src/features/network-trace/TraceViewControls.tsx` rendering `Group`, `Order`, `Min Δ` with its 200 ms debounce, hint and `Clear`, the hidden pill, the warnings pill (`trace-warnings-pill`: count, messages in `title` and `aria-label`, absent when empty) and `TraceLegend`, with today's test ids; verify `TraceViewControls.test.tsx` carries the debounce, blur-normalisation, hidden-pill and legend-row tests moved from `TraceView.test.tsx` and adds "Warnings are a pill, not a drawer"

## 3. The page owns the projection (design D3, D5; spec `storage-flow-sankey`, `network-trace`)

- [x] 3.1 Add `useSankeyProjection({ elements, mode, topPods, roots })` to `storage-flow-sankey` returning the cut elements, `podCut` and `svmAvailable`, exported from the barrel; verify `useSankeyProjection.test.ts` carries the cut and `svmAvailable` cases moved from `SankeyView.test.tsx` ("The default cut bounds a large body", "A pod root disables the cut", "K beyond the pod count hides nothing", availability with and without `labels.aggr`)
- [x] 3.2 Slim `SankeyView.tsx`: delete the title-bar row, the `summary` memo and `SankeySummary` (with `SankeySummary.test.tsx`), the cut, the `svmAvailable` memo and the local-state fallbacks; take `podCut`, `svmAvailable` and required `mode` / `podLayout` / `svmDisplay`; verify `SankeyView.test.tsx` renders with cut elements, no longer finds `sankey-summary` or a `Storage flow` eyebrow, and its empty-state, hover, tooltip and keyboard cases still pass
- [x] 3.3 In `SankeyPage.tsx` call the hook, pass its result to `SankeyView`, and render `SankeyScopeBar` with `trailing={<SankeyViewControls …/>}`; verify `AppShell.test.tsx` covers "Control bar follows the view switch" (Locate then Back returns the bar at `Both` / `Flat` / `Column`), that the layout switch still issues no request, that focus mode hides the scope bar with its controls, and that the controls are operable while `sankey-empty-awaiting` is shown
- [x] 3.4 Add `useTraceModel({ elements, trackDir, minBps, grouping })` to `network-trace` returning `direction` and `model`; slim `TraceView.tsx` to take `model`, `direction` and `order`, delete its title-bar row, `TraceSummary` (with its test), the `order` state, the `warnings` memo and `onLocateNode`; verify `useTraceModel.test.ts` covers the direction warning and "Derivation does not change the source data", and `TraceView.test.tsx` keeps its empty-state, chart, hover and keyboard cases against a passed-in model
- [x] 3.5 In `NetworkPage.tsx` call `useTraceModel`, hold `order` beside `grouping`, build the warnings list from `direction.warning`, `model.warnings` and the loader's `errors`, and render `TraceScopeBar` with `trailing={<TraceViewControls …/>}`; verify `AppShell.test.tsx` covers "The trace scope bar stays across the Network views" (switching `Group` / `Order` keeps the draft and dirty state), "Reload is unavailable on the Network page without a valid draft or endpoint", and "Focus mode on the Network Sankey" hiding the scope bar
- [x] 3.6 Delete `src/shared/ui/SummaryPanel.tsx`; verify `grep -r SummaryPanel src` finds nothing and `npm run typecheck` passes

## 4. The storage chart follows the trace (design D6, D7, D8, D9; spec `storage-flow-sankey`, `sankey-canvas`, `network-trace`)

- [x] 4.1 Add `cardHeaderH(lines)` to `sankey-canvas/geometry.ts` and use it from the trace's `hopHeaderH` / `leafCardH`; verify `layoutTrace.test.ts` and the trace `golden.test.ts` are unchanged
- [x] 4.2 Add `cardText(node, flow)` to `layoutSankey.ts` (kind-only subtitle with `· <ontap_cluster>` / `· no flow`; lines `ns/…`, `usage …` through `formatUsage`, `<n> pods`, `total …`), carry `extraLines` on `LayoutNode`, size the header with `cardHeaderH` and place both slot stacks below it, and pass `extraLines` from `SankeyChart` to `SankeyCard`; verify `layoutSankey.test.ts` covers "The three rows of a pvc box card", "A card missing usage does not fill in zero" (one line step shorter), "namespace is a leaf card", the `netapp-node · ontap-prod` subtitle and the `· no flow` subtitle, and `SankeyView.test.tsx` covers "The shared card draws the same SVG"
- [x] 4.3 Move the chevron to `sankey-canvas`: `endChevronPath(x2, y2, thickness, dir)` in `geometry.ts` and a `RibbonChevron` component, imported by `network-trace/layout/paths.ts` and `chart/TraceBand.tsx`; record `LayoutLink.chevron` in `layoutSankey` from the target slot centre and draw `<RibbonChevron testId="sankey-link-chevron">` after every ribbon in `SankeyChart`; verify a geometry unit test pins the path for a 20 px and a 4 px ribbon, `SankeyView.test.tsx` covers "Every ribbon ends in a chevron" (one per ribbon, faded with its ribbon on hover), and the trace golden test and `trace-band-chevron` assertions are unchanged
- [x] 4.4 Give the storage tooltip builders `tokens` and emit the flow rows and the link's direction row as `{ text, color }` in the read / write tokens, derived, wrapper and frame rows included; verify `SankeyView.test.tsx` covers "Flow rows are painted like their ribbons" for a card in Both mode, a write link, a derived card and a wrapper, and that `usage`, `status` and perf rows stay plain
- [x] 4.5 Make every Network Sankey card non-locatable: delete `network-trace/model/locatable.ts` and its test, pass `locatable={false}` from `TraceCard`, drop the `onNodeClick` plumbing; verify `TraceCards.test.tsx` asserts `data-locatable="false"` and no pointer cursor on a hop, a pod and a leaf, and "Clicking a hop card does nothing" passes in `AppShell.test.tsx`

## 5. End-to-end (Playwright, `tests/`)

- [x] 5.1 Rewrite `tests/network-trace.spec.ts`: drop every `nav-view` click and Graph-view expectation, assert the `Min Δ` controls and legend inside `trace-controls`, add `/network/graph?hostname=…` → not-found with no trace request, `/network?hostname=…` → `/network/sankey` keeping the query, the unconfigured state on `/network/sankey` alone, and the demo-mode fixture without a Graph click; verify `npm run e2e` passes
- [x] 5.2 Update `tests/storage-graph.spec.ts` and `tests/demo.spec.ts`: read the cut from `sankey-top-pods-label` (`10 of 15 pods`, then absent), drop the summary toggle and `sankey-summary` assertions, replace the nav-link round trip with Locate and `goBack`, assert the navigation landmark holds no link, one `sankey-link-chevron` per ribbon, and a coloured `in read` tooltip row; verify `npm run e2e` passes
- [x] 5.3 Run the whole suite including `tests/url-scope.spec.ts` (Locate then Back, unchanged) and `tests/sankey-svm-grouping.spec.ts` (the `SVM` control now inside `sankey-controls`); verify `npm run e2e` passes

## 6. Documentation and gates

- [x] 6.1 Update `README.md`: the nav-bar paragraph (three standalone pages, no category / view control), the `/network/sankey` section (heading, `/network` redirect target, no Graph view, cards not clickable), the Sankey summary paragraph (removed; the cut statement beside Top pods), and the card / tooltip description (attribute lines, painted rows, chevrons); verify the text reads against the delta specs
- [x] 6.2 Run `npm run lint`, `npm run typecheck`, `npm run test:ci`, `npm run e2e` and `npm run fixture:check`; verify all pass with coverage at or above 80% on the changed modules
- [x] 6.3 Run `openspec validate standalone-pages-lean-sankey --strict`; verify it passes and that every scenario in the seven delta specs maps to a test from groups 1–5
- [ ] 6.4 After merge, in `kube-state-graph-demo`: drop `/network/graph` from `scripts/verify.sh` §10 (assert the not-found document for it instead), update `CLAUDE.md`'s route description, and move the submodule pointer; verify `make verify` reports 0 failed — **deferred to `kube-state-graph-demo` after merge**, as the task says: the submodule pointer can only move to a merged commit, and `scripts/verify.sh` §10 must keep matching the image the demo actually deploys. Nothing in this repository still serves `/network/graph` (`routes.test.ts` and `tests/network-trace.spec.ts` assert it answers the not-found page).
