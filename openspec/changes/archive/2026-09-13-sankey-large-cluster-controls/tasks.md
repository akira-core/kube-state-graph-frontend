## 1. Loader and shell plumbing (design D1, D4)

- [x] 1.1 Rewrite `src/features/graph-data/hooks/useGraphLoader.ts` to the imperative API `{ state, run(makeUrl), reload(), cancel() }` with `state.cancelled`, no `requestKey` effect and no fetch on mount; verify `useGraphLoader.test.ts` covers: mount issues 0 requests, `run` issues 1, `reload` before `run` is inert, `cancel` aborts and keeps `elements` / `hasPayload` with `status` `ready` (or `idle` with no payload) and `error` undefined, a late response after `cancel` / unmount changes nothing, and at most one in-flight request (`run` / `reload` while in flight is a no-op)
- [x] 1.2 Move the auto-refresh timer into the loader — started / restarted on every successful `run` / `reload`, cleared on `cancel` and unmount, tick skipped while in flight; verify with fake timers in `useGraphLoader.test.ts` that no tick fires before the first `run`, ticks fire every `refreshIntervalSeconds` afterwards, and none fires after `cancel` until the next `run` / `reload`
- [x] 1.3 Add `src/shared/ui/QueryButton.tsx` (`{ dirty, inFlight, disabled, disabledReason, onQuery, onCancel }`: renders `Query`, accented while `dirty`; renders `Cancel` while `inFlight`; shows `disabledReason` beside it when disabled; keyboard-operable with an accessible name) and verify `QueryButton.test.tsx` covers all four states and that Cancel is clickable while disabled-by-scope would otherwise apply
- [x] 1.4 Extend `PageStatus` in `src/features/app-shell/ShellFrame.tsx` with `phase: 'awaiting' | 'loading' | 'ready' | 'error' | 'cancelled'` and teach `NavBar.tsx` to render the awaiting-Query and cancelled readouts, keep the last load time visible while cancelled, and disable Reload while `phase` is `awaiting` or `loading`; verify `NavBar.test.tsx` covers the two new readouts and the disabled states

## 2. Applied scope, draft and the single URL writer (design D2, D3)

- [x] 2.1 Replace `src/features/app-shell/useUrlScope.ts` with `useAppliedScope(parse, serialize)` returning `{ applied, commit(scope, range) }` where `commit` writes scope pairs, immediate view values and `from` / `to` in one replace and never writes on render; verify `useAppliedScope.test.tsx` asserts one `replaceState` per commit, zero writes on mount when the URL already carries a valid `from` / `to`, and that unknown parameters are absent after a commit
- [x] 2.2 Add `useDraft(applied)` (seeds from `applied` on mount, re-seeds when `applied` changes without a local commit, exposes `dirty` by deep equality); verify `useDraft.test.tsx` covers seed on mount, `dirty` toggling on edit and after commit, and re-seed on an external URL change
- [x] 2.3 Rewrite `src/features/app-shell/useViewTimeRange.ts` to hold only the draft range plus `seedDraft` / local-storage helpers — no `setSearchParams`, no URL → state effect; verify `useViewTimeRange.test.tsx` asserts it never calls `history.replaceState` and that `seedDraft` updates the control value
- [x] 2.4 Add the revert regression test: render the shell under `MemoryRouter`, select `1h` with the navigation wrapped in `startTransition`, and assert the control still reads `1h` after settling, that the query string was not rewritten, and that no request was issued (spec `app-shell` › "A selected window sticks")
- [x] 2.5 Mount behaviour in both pages: parse `from` / `to` on mount, `seedDraft` when valid, otherwise commit once with the shell draft; verify page tests assert exactly one replace on a bare path, zero on a path with a valid pair, and zero requests either way

## 3. Graph page (specs `explicit-query`, `graph-filters`, `graph-data-source`)

- [x] 3.1 Wire `GraphPage.tsx` to `useAppliedScope` + `useDraft` + the imperative loader: Query commits `{ draft, draftRange }` then calls `run` with a builder over the committed values; Reload calls `loader.reload`; publish `phase` / `reload` / `reloadDisabled` to the shell; verify `AppShell.test.tsx` covers mount = 0 requests, edit = 0 requests, Query = 1 request with the committed parameters, Reload before commit inert
- [x] 3.2 Add `QueryButton` to `FilterBar.tsx` as the last labelled column, pass `dirty` / `inFlight`, make Clear reset the draft only (disabled when the draft is at default); verify `FilterBar.test.tsx` covers the Query / Cancel rendering, the dirty accent, and that Clear neither issues a request nor changes the URL
- [x] 3.3 Add the awaiting-Query state to `GraphView` (rendered when `status` is `idle` and `hasPayload` is false, naming the Query control) and the cancelled-with-no-data variant; verify a `GraphView` test renders the explanation instead of the loading overlay and the e2e in 8.1 sees it on a fresh mount
- [x] 3.4 Update `graphRequestUrl.ts` / `storageGraphRequestUrl.ts` consumers so the request key concept is gone and `start` / `end` are still resolved at send time; verify the existing "relative window re-resolved on every request" tests pass against `run` + `reload`

## 4. Sankey scope: required multi-valued roots and candidates (design D5, D6)

- [x] 4.1 Add `allRow?: boolean` (default `true`) to `src/shared/ui/ScopeSelect.tsx`; verify `ScopeSelect.test.tsx` asserts the `All` row is absent when `allRow={false}` in multi mode and every other multi-select behaviour is unchanged
- [x] 4.2 Extend `labelValuesUrl(base, label, selector?)` in `src/features/graph-filters/labelValues.ts` to render `match[]=kube_pod_info{namespace="<escaped>"}` with PromQL escaping of `\` and `"`, URL-encoded, and `fetchLabelValues` to accept the selector; verify `labelValues.test.ts` covers the encoded URL for a plain and a quote-carrying namespace
- [x] 4.3 Add `useRootCandidates({ labelValuesBase, kind, namespaces, drawn })` in `src/features/storage-flow-sankey/useRootCandidates.ts`: `node` values fetched once per mount when the kind is `node`, `pod` values fetched per namespace when the kind is `pod`, cached per key, unioned with `rootValueOptions(drawn)`, problems surfaced; verify `useRootCandidates.test.ts` asserts request counts (0 before the kind is chosen, 1 per namespace, no refetch on kind toggling), the `<ns>/<pod>` shape, and that a failed source yields an empty list plus a problem line
- [x] 4.4 Rework the root controls in `SankeyScopeBar.tsx`: multi-select root value (`allRow={false}`, `emptyLabel="Pick values to add"`), `Add` committing every pending value via `addRoots(kind, values[])`, pending cleared on kind change, whole Add refused with the inline error when any `pod` value is malformed, and the empty-list explanations per kind (pick a namespace / typed until drawn); verify `SankeyScopeBar.test.tsx` covers multi-add, kind-change clearing, the refused Add, and the three explanations
- [x] 4.5 Make the root required: Query unavailable with the "root required" text while the draft holds no root, `reloadDisabled` while `az` / `env` / root are not all present, the scope bar row holding `AZ`, `Env`, `Root kind`, `Root value`, `Add`, `Top pods`, `Query` on one baseline; verify `SankeyScopeBar.test.tsx` asserts the disabled reason and `SankeyView.test.tsx` the "Scope incomplete" empty state naming the root
- [x] 4.6 Add `topPods` to `sankeyUrlScope.ts` (parse: integer ≥ 1 else `10`; serialise only when ≠ `10` and no `pod` root) and keep `mode` / `top_pods` writes going through `commit`; verify `sankeyUrlScope.test.ts` covers the round-trip, the fallback on garbage, and omission with a `pod` root
- [x] 4.7 Wire `SankeyPage.tsx` to `useAppliedScope` + `useDraft` + the imperative loader, auto-preselect seeding the draft only, Query committing scope + range then `run`, the "Awaiting Query" and "Cancelled" empty states, and demo mode holding `topPods` in component state; verify `SankeyView.test.tsx` / `AppShell.test.tsx` cover the six empty states by cause and that a complete deep link issues 0 requests until Query

## 5. Top pods projection (design D7)

- [x] 5.1 Implement `cutTopPods(elements, mode, k)` in `src/features/storage-flow-sankey/topPods.ts` returning `{ elements, shown, total }` — ranking by inbound `pvc-pod` totals in the mode's directions, ties by label, backward reachability over the four tiers, `pod-node` edges of dropped pods removed, no-flow pods untouched, input never mutated; verify `topPods.test.ts` on the performance fixture asserts membership counts, every surviving edge's metrics deep-equal the input's, `shown` / `total`, the mode re-ranking case, `k ≥ total` as identity, and deep-equality of the input before and after
- [x] 5.2 Apply the cut in `SankeyView.tsx` before `deriveSankey`, gated on the applied roots holding no `pod` root; build `Node`-layout wrappers from the cut elements; verify `SankeyView.test.tsx` asserts 10 pod cards on the large fixture by default, the whole body with a `pod` root, and a wrapper left without kept pods is not drawn
- [x] 5.3 Add the `Top pods` control to `SankeyScopeBar.tsx` (integer ≥ 1, default 10, disabled with a reason while the draft has a `pod` root, redraw without a request) and the toolbar label `Top K pods` while a cut is in effect; verify `SankeyScopeBar.test.tsx` and `SankeyView.test.tsx` cover the disabled state, the immediate redraw with 0 requests, and the toolbar label appearing only when pods were hidden
- [x] 5.4 State the cut in `SankeySummary.tsx`'s header strip (`K of N pods`) and in the empty-state hints; verify `SankeySummary` tests assert the statement appears only when `shown < total`
- [x] 5.5 Extend `sankeyPerformance.test.ts`: the 3000-edge first draw bound holds with Top pods at `1000`, and with the default `10` the cut completes within 100 ms and the first draw within 300 ms

## 6. Derived cards carry no status (design D8)

- [x] 6.1 Stop assigning `status` to synthesised `application` / `namespace` nodes in `deriveSankey.ts`; verify `deriveSankey.test.ts` asserts those nodes have no `status` while a member pod is `warning`, and the wrapper fold in `layoutSankey.test.ts` still yields `warning`
- [x] 6.2 Confirm the downstream consumers follow: neutral border on derived cards, placeholder in the summary status column, no status item in their tooltip, wrapper tooltip keeps "worst of members"; verify `SankeyView.test.tsx` / `SankeySummary` tests cover the spec scenarios "A container borders by the worst status it hides", "Derived rows carry no status" and "Hovering a derived card and a derived link"

## 7. Front door label rebinding (design D9)

- [x] 7.1 Extend `docker/entrypoint.sh`: read `KSG_AZ_LABEL` / `KSG_ENV_LABEL` (defaults `az` / `env`), validate each against `[A-Za-z_][A-Za-z0-9_]*` and reject equality with a message naming the variable, before and regardless of `KSG_METRICS_PROXY_TARGET`; verify by running the script's validation with `sh -n` and with `KSG_AZ_LABEL='zone; }'` and `KSG_AZ_LABEL=site KSG_ENV_LABEL=site` that it exits non-zero naming the variable
- [x] 7.2 Emit exact-match `location = /metrics-api/api/v1/label/az/values` / `…/env/values` blocks (via `proxy_location` extended with an exact flag; a static `proxy_pass` keeps the query string, so no `$is_args$args` and no runtime `resolver`) ahead of the prefix block when the metrics target is set and a binding differs from its default; verify `nginx -t` passes inside the image and `make image` builds
- [x] 7.3 Run the container matrix against a stub upstream that echoes the request path: with `KSG_AZ_LABEL=zone KSG_ENV_LABEL=environment`, `GET /metrics-api/api/v1/label/az/values?match%5B%5D=kube_pod_info` reaches `/api/v1/label/zone/values?match[]=kube_pod_info`, `env` reaches `environment`, `namespace` is unchanged; with the variables unset all three pass through unchanged; `/metrics-api/api/v1/query` still 404s; record the commands and results in the PR description
- [x] 7.4 Document the two variables in `deploy/deployment.yaml` (commented env directly after `KSG_METRICS_PROXY_TARGET`, stating they must equal the backend's `--az-label` / `--env-label`), `deploy/README.md` (what they rewrite, parity with the backend, the mismatch symptom) and `README.md` › Connecting to a backend; verify `kubectl apply -k deploy/ --dry-run=client` succeeds and the README renders the new section

## 8. End-to-end coverage (Playwright, `tests/`)

- [x] 8.1 Add `tests/explicit-query.spec.ts`: on `/graph` and `/sankey` a mount with a complete URL scope issues 0 requests and shows the awaiting-Query state; Query issues exactly 1 request carrying the committed parameters and replaces the URL; editing afterwards issues 0 requests and marks the draft dirty; Cancel during a slow response keeps the previous drawing and stops auto-refresh; Reload is disabled before the first commit; verify the spec passes with `npm run e2e`
- [x] 8.2 Extend `tests/storage-graph.spec.ts`: Query unavailable without a root, multi-value Add, a `pod` root disabling Top pods, the default cut drawing 10 pods with the `10 of N pods` strip, `top_pods=25` written on change without a request; verify it passes
- [x] 8.3 Extend `tests/url-scope.spec.ts` with "A selected window sticks" (select `1h`, wait 2 s, control still `1h`, URL unchanged, 0 requests; Query → one request with a 1-hour window and `from=now-1h&to=now`) and the deep-link / Back prefills-without-request scenarios; verify it passes

## 9. Documentation and final gates

- [x] 9.1 Update `README.md` › URL parameters (`top_pods`, URL = applied scope, commit semantics), › Troubleshooting ("nothing draws after opening a link — press Query"; "az / env empty with renamed labels — set `KSG_AZ_LABEL` / `KSG_ENV_LABEL`"), and the Sankey root-required note; verify the sections read against the specs' wording
- [x] 9.2 Run `make check` (lint, typecheck, fixture check, unit tests with coverage ≥ 80% on the new modules) and `npm run e2e`; verify both are green and record counts in the PR description
- [x] 9.3 Build and scan the image (`make image`, `make scan`), start it with `demoMode: true` and confirm both routes render the fixtures on mount with no Query control and no CSP violation; verify with the browser console and the container logs
- [x] 9.4 Run `openspec validate sankey-large-cluster-controls --strict` and `/opsx:verify`; verify every scenario in the six delta specs maps to a passing test or a recorded manual check before archiving

## 10. Scope bar follow-ups (after review)

- [x] 10.1 Drop the standing prose under the Sankey scope bar — "`node` matches both…", the intersection note and the Flat-layout Kubernetes-node hint — together with `kubernetesNodeRoots`, which only fed the hint; verify `SankeyScopeBar.test.tsx` asserts the prose is absent and the delta spec's "Layout switch" requirement no longer asks for the hint
- [x] 10.2 Make Query read as an action rather than one more field: no label above it, a filled button after a divider at the row's end (`solid` when clean, `primary` while dirty, `outline` with a spinner as Cancel), and "Changes not applied" beside it while dirty; verify `QueryButton.test.tsx` and screenshots of both bars in light and dark
- [x] 10.3 Replace the root value Add step with direct checking: the dropdown's checked set is the current kind's draft roots; verify `SankeyScopeBar.test.tsx`, `AppShell.test.tsx` and `tests/storage-graph.spec.ts` pass with no Add button
