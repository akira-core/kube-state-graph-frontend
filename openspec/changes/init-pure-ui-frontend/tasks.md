## 1. Project skeleton and toolchain

- [x] 1.1 Initialize the project with Vite + React 18 + TypeScript (`package.json`, `vite.config.ts`, `tsconfig.json` / `tsconfig.build.json`, `index.html`, `src/main.tsx`), add `.nvmrc` (Node 22); Verify: `npm install && npm run dev` starts and the browser shows a placeholder page
- [x] 1.2 Set up `npm run typecheck` (`tsc --noEmit`) and `npm run build` (typecheck runs before build); Verify: both commands exit with code 0, `dist/` produces static assets
- [x] 1.3 Add the ESLint config (port the panel's rule set, remove `@grafana/eslint-config`, keep the feature-boundary rules `import-x/no-default-export` and `import-x/no-restricted-paths`); Verify: `npm run lint` passes with zero warnings on the empty project
- [x] 1.4 Add the Prettier config and `.prettierignore` (including `public/demo/graph.json`); Verify: `npm run format` is idempotent, rerunning produces no diff
- [x] 1.5 Set up Vitest (`npm run test` / `npm run test:ci`) and Testing Library; Verify: one placeholder test passes and `test:ci` produces a coverage report
- [x] 1.6 Set up Playwright (`playwright.config.ts`, `webServer` starts the dev server itself); Verify: `npm run e2e` can launch a browser and run one placeholder spec through
- [x] 1.7 Add version-controlled git hooks (`.githooks/`, `init-hooks`, `prepare`, lint-staged): pre-commit runs lint-staged, pre-push runs `typecheck` + `fixture:check`; Verify: after `npm run init-hooks`, introduce a lint error and the commit is blocked
- [x] 1.8 Add Makefile shortcuts (`make dev` / `test` / `lint` / `build`); Verify: each target runs the corresponding npm script successfully
- [x] 1.9 Install runtime dependencies: `cytoscape`, `cytoscape-fcose`, `cytoscape-dagre`, `cytoscape-expand-collapse`, `d3-sankey`, `react-router`, `tailwindcss`, `clsx`, `cva`, `@radix-ui/react-dropdown-menu`, `@radix-ui/react-tooltip`, `@radix-ui/react-toggle-group`; Verify: `npm ls` shows no peer dependency warnings, `npm run build` succeeds
- [x] 1.10 Set up Tailwind (`tailwind.config.ts`, base CSS, class-based dark mode); Verify: an element with a Tailwind class is styled correctly under `npm run dev`, and colors change after toggling the `dark` class on `<html>`

## 2. Design tokens and theme layer

- [x] 2.1 Create `src/shared/theme/tokens.ts`: define the light / dark token sets as TypeScript objects (background and foreground hierarchy, semantic status colors, kind colors, edge type colors, Sankey read / write colors); port the color values from the panel's `shared/theme/themeColors.ts`, replacing the `GrafanaTheme2` dependency with an in-house type; Verify: a unit test asserts the key sets of the two token sets are identical, with no missing keys
- [x] 2.2 Implement the mechanism that produces CSS variables from tokens and injects them into `:root` and `.dark`; Verify: a unit test asserts the produced CSS variable names correspond one-to-one with the token keys
- [x] 2.3 Implement the theme resolution and application hook (effective theme = locally saved user choice → config file `theme` → `system`; `system` follows the operating system preference and reflects it live); Verify: component tests cover the three-level precedence and live application of a `system` preference change
- [x] 2.4 Port all colors and mapping tables from `shared/constants/**` (`colorByEdgeType`, `colorByStatus`, `colorBySeverity`, `colorByResultType`, `iconSvgByKind`, `categoryByKind`, each palette, `edgeRelation`, `drawnEdgeTypesForMode`, `ingressGateway`, `missingValuePlaceholder`, `applicationBearingKinds`, `types`), with color values now pointing at `tokens.ts`; Verify: all ported mapping tests pass

## 3. Runtime config

- [x] 3.1 Define the TypeScript types for runtime config (`endpoints.graph` / `codeChanges` / `configChanges` / `dashboard`, `demoMode`, `refreshIntervalSeconds`, `defaultLayout`, `theme`); Verify: the type file passes `npm run typecheck`
- [x] 3.2 Implement the hand-written config validator: no type coercion, URLs accept only absolute `http(s)` or root-relative with a single leading slash, reject protocol-relative, treat `null` as a type error, ignore unknown keys with a warning, report the first problem; Verify: unit tests cover valid values, invalid values and defaults for every field, plus every URL-form case in the `runtime-config` spec
- [x] 3.3 Implement loading `<base>/config.json` at startup (completed before any view renders, the path cannot be overridden by the page URL's query / hash, any non-2xx is a failure); Verify: unit tests cover the four paths: success, 404, invalid JSON, validation failure
- [x] 3.4 Implement the full-screen configuration error screen (shows the config path and the first problem, offers Retry, never silently falls back to demo mode); Verify: component tests assert both 404 and validation failure render the error screen rather than demo data, and that Retry re-fetches
- [x] 3.5 Create `dev/config.json` (`demoMode: true`, version-controlled) and the `dev/config.local.json` override mechanism (gitignored), and configure the `/api/`-prefixed dev proxy in `vite.config.ts` (target given by `KSG_DEV_PROXY_TARGET`); Verify: `npm run dev` enters demo mode on a clean checkout; after setting `dev/config.local.json` that file is read instead; `dist/` contains no `config.json`

## 4. Pure logic port — shared

- [x] 4.1 Port `shared/types/**` (`wire.ts`, `cytoscape.d.ts`, `cytoscape-extensions.d.ts`, `containerSpec.ts`) and `shared/guards/isPlainObject.ts` as-is; Verify: `npm run typecheck` passes
- [x] 4.2 Port `shared/graph/**` (`childrenByParent`, `collapsedAncestors`, `collectIngressNodeIds`), `shared/clone/clonePlain.ts`, `shared/format/measurements.ts` and their tests as-is; Verify: all ported unit tests pass
- [x] 4.3 Port `shared/nodeAttributes/buildNodeAttributes.ts` and `shared/icon/**` (`tintSvgToDataUri`, `paintUsageLiquid`) and their tests as-is; Verify: all ported unit tests pass

## 5. Pure logic port — feature

- [x] 5.1 Port `features/graph-data/` (`normalize.ts`, `wrapNodeGroup.ts`, `wrapSwitchFabric.ts`) and its tests as-is; Verify: all normalize unit tests pass (including alerts / controller aggregation / both edge-metrics branches / NetApp and PVC fields / `ready_status`)
- [x] 5.2 Port `features/element-filter/computeVisibility.ts`, `features/pod-parent-mode/applyPodParentMode.ts`, `features/switch-topology/**` (`readSwitchLevels`, `buildSwitchConstraints`) and their tests as-is; Verify: all ported unit tests pass
- [x] 5.3 Port the pure logic of `features/graph-search/` (`computeHits`, `resolveSearchHits`, `keyboardNav`, `types`) and its tests as-is; Verify: all ported unit tests pass
- [x] 5.4 Port the pure logic of `features/node-detail/` (`parseDashboardLinks`, `detailUrlKinds`, `detailPaths`) and its tests as-is; Verify: all ported unit tests pass
- [x] 5.5 Port `features/graph-canvas/sync/**` (`diffElements`, `cloneElementDefs`, `reconcileCollapse`, `seedAddedNodePositions`, `extensionDataKeys`) plus `clusterCollapseToggle`, `selectSingle` and their tests as-is; Verify: all ported unit tests pass
- [x] 5.6 Rewrite `assembleDashboardParams.ts`: replace the `TimeRange` dependency with the in-house view time range type, leaving the rest of the parameter assembly logic unchanged; Verify: the ported tests pass after switching to the new type, covering applicability per kind and returning "no parameters" when not applicable
- [x] 5.7 Rewrite `formatEdgeMetrics.ts` and `formatChangeTime.ts`: remove the `@grafana/data` dependency, switch the time zone to the browser's local time zone; Verify: the ported tests pass, including the case that a tiny value (such as `3.86e-7`) is not truncated to 0
- [x] 5.8 Rewrite endpoint resolution: replace datasource lookup with the runtime config's `endpoints.*`; an endpoint that is not configured is "unavailable"; Verify: a unit test asserts an unconfigured endpoint issues no request and the corresponding UI does not render

## 6. Fixture and demo data

- [x] 6.1 Port `shared/fixtures/showcaseGraph.ts` (typed as `WireGraph`) and its tests as-is; Verify: `npm run typecheck` passes, fixture tests pass
- [x] 6.2 Extend the fixture to cover the cases Sankey needs: at least one `pvc-to-netapp-aggr` edge carrying both `read_bytes_per_sec` and `write_bytes_per_sec`, and one edge carrying neither; Verify: a unit test asserts both kinds of edge exist
- [x] 6.3 Implement `fixture:build` (serialize `SHOWCASE_GRAPH` as a complete `GET /v1/graph` response body, output to `public/demo/graph.json`) and `fixture:check` (drift check); Verify: `npm run fixture:check` passes after `npm run fixture:build`; editing the fixture by hand without rebuilding makes `fixture:check` fail

## 7. App shell and data lifecycle

- [x] 7.1 Implement the app entry point and startup sequence (loading screen → configuration error screen or app; no request is issued to any backend endpoint before config loading completes); Verify: a component test asserts no fetch is issued while config is not yet loaded
- [x] 7.2 Implement routing with react-router: `/graph`, `/sankey`, `/` redirects to `/graph` via history replace, trailing slashes tolerated, unknown paths show a not-found page screen (shell retained, with a back link); Verify: component tests cover the four paths, e2e covers that a direct refresh on `/sankey` works
- [x] 7.3 Implement the top nav bar: two view links (with `aria-current`), theme toggle, reload action, last-loaded-time status indicator, demo mode badge; Verify: component tests assert every control has an accessible name and supports keyboard operation, and the badge appears in demo mode
- [x] 7.4 Implement the shared graph data loading hook: a single in-flight request, stale-while-revalidate (keep the last successful data during refresh and after failure), failure surfaced only through the status indicator, manual reload resets the auto-refresh interval, the timer is enabled only when `refreshIntervalSeconds > 0`, demo mode issues no network request; Verify: unit tests cover these six semantics
- [x] 7.5 Implement the view time range control (`1h` / `6h` / `24h` / `7d` / custom absolute window, default `24h`, saved in browser local storage, not in the URL, relative windows resolved to now at read time); Verify: component tests cover the default, persistence, resolution at read time, and that changes do not trigger a graph fetch
- [x] 7.6 Implement the view mounting strategy: mount on first entry, later switches only toggle visibility, switching back to visible triggers a size recomputation; Verify: component tests assert switching views does not refetch, does not re-normalize, and each view's ephemeral state is preserved
- [x] 7.7 Implement the view area filling the remaining viewport height plus accessibility basics (nav / main landmarks, focus-visible); Verify: component tests assert the landmarks exist, and the view area height changes after the window is resized

## 8. Graph canvas foundation

- [x] 8.1 Port and rewrite `registerExtensions.ts` (register fcose / dagre / expand-collapse at module top level, registered only once); Verify: a unit test asserts repeated imports produce no duplicate-registration warning
- [x] 8.2 Port the `useCytoscape` hook and its lifecycle contract (init and update effects split, cleanup calls `removeAllListeners()` + `destroy()` and zeroes the ref, idempotent under StrictMode double-mount); Verify: a component test walks mount → unmount → re-mount under StrictMode, asserting no listener residue and no duplicate-registration warning
- [x] 8.3 Rewrite `getStylesheet.ts`: the theme parameter changes from `GrafanaTheme2` to the token type of `tokens.ts`, a pure factory producing `Stylesheet[]`; Verify: the ported snapshot tests pass after switching to the new tokens
- [x] 8.4 Port `useGraphLayout` and `useLayoutRunToken`: `layout().run()` only after `cy.stop()`, options stabilized with `useMemo`, initial algorithm taken from the runtime config `defaultLayout`; Verify: unit tests assert identical input does not rerun the layout, and an input change runs it exactly once
- [x] 8.5 Port `useGraphResize` (`ResizeObserver` + debounced `cy.resize()`); Verify: a component test with a mock ResizeObserver asserts a size change triggers `resize()` once
- [x] 8.6 Implement element sync: ordinary data refreshes go through diff-and-patch, pod-parent mode switches go through a full rebuild; Verify: unit tests assert an N → N+M refresh does not remove / re-add the existing N nodes, while a mode switch rebuilds everything
- [x] 8.7 Implement the canvas component and the in-app layout algorithm switch control (`fcose` / `dagre`, ephemeral view state); Verify: e2e renders the complete graph in demo mode, and switching the algorithm re-lays out the graph without rebuilding the instance
- [x] 8.8 Implement loading / empty / error state rendering (including the partial-parse warning banner placed in the top-left corner); Verify: component tests cover the four states, and assert elements still render on partial-parse with only the warning banner shown

## 9. Graph view — legend and filtering

- [x] 9.1 Rewrite the legend section components with Tailwind + Radix (node kinds, edge types, status, cluster, namespace, application, container, swatch, icon glyph, edge glyph); Verify: component tests assert each section renders or does not render according to the spec's data conditions
- [x] 9.2 Implement the per-row show / hide toggle (eye / eye-slash) and the `visibleKinds` / `visibleEdgeTypes` view state, wired to the visibility computation (including orphan cascade); Verify: component tests cover changes to the visible set of canvas elements after toggling, and the determinism of the visibility computation as a pure function
- [x] 9.3 Implement the collapse-aware node-kinds legend (lists only kinds actually drawn); Verify: a component test asserts the legend list changes after a container is collapsed
- [x] 9.4 Implement collapsing the legend to the side: the collapse button sits in the action slot at the right end of the Layout row; when collapsed, only a floating restore button renders, whose `z-index` is above the expand-collapse overlay canvas but below the app shell's nav and global overlays; Verify: e2e asserts the canvas regains width after collapse, and the restore button takes effect on click

## 10. Graph view — interaction

- [x] 10.1 Implement selection state and focus fade (selected node + incident edges + neighbors + descendants + ancestor containers of the above); Verify: component tests assert the highlighted set is correct, and no highlighted edge terminates at a faded node
- [x] 10.2 Implement the state projection from canvas events into React (handlers registered once in the init effect, subscribed via `useSyncExternalStore`); Verify: a component test asserts prop changes do not re-register listeners
- [x] 10.3 Rewrite the hover tooltip with Tailwind (follows the cursor, node and edge content variants, edge covers both the RED and storage I/O metric families); Verify: component tests cover presentation of both metric families and degradation on missing fields (absent is not shown as 0)
- [x] 10.4 Implement the pinned card (shares its content source with the hover tooltip, docks below the search bar, the two never overlap); Verify: a component test asserts the relative position and non-overlap when both are present
- [x] 10.5 Port `useExpandCollapse` and collapse reconciliation (desired ∩ present), including controllers collapsed by default on entering controller mode; Verify: the ported tests pass, and assert the collapse set is preserved after a data refresh
- [x] 10.6 Implement the status outline and node usage visuals (including selector-order overrides for compound parents); Verify: component tests assert K8s nodes and controllers still show the status outline when acting as compound parents

## 11. Graph view — topology features

- [x] 11.1 Implement the pod-parent mode segmented control (`Node` / `Controller`, placed in the Layout row at the top of the legend, default `controller`, ephemeral view state) wired to the topology transform; Verify: e2e asserts the pods' compound parent chain changes after switching mode, and the presentation of `pod-to-node` changes with it
- [x] 11.2 Implement the drawable edge set and edge type colors per mode; Verify: unit tests assert the drawable sets of both modes are correct, and that `pod-runs-on-node` / `controller-owns-pod` are not synthesized
- [x] 11.3 Implement the ingress display toggle and dashed style (`ingress-gateway` set recognition, `showIngress` ephemeral view state defaulting to `true`, `ingress-lb` not in the set); Verify: the ported tests pass, e2e asserts the gateway chain is hidden after toggling while `ingress-lb` remains
- [x] 11.4 Wire up the switch tier layout constraints (read the level from `data.labels.level`, apply fixed node positions so switches stack by tier); Verify: e2e asserts the switches in the fixture are arranged top to bottom by level
- [x] 11.5 Wire up node group compound synthesis (`cluster > node group > node`); Verify: the ported tests pass, e2e asserts the node group container exists and can be collapsed

## 12. Graph search

- [x] 12.1 Rewrite the search bar with Tailwind + Radix (always present in the canvas's top-right corner, `right: 8`, takes no layout space, stacked above the pinned card, does not overlap the warning banner); Verify: component tests assert positioning and stacking relationships
- [x] 12.2 Implement the result list (main row label + kind badge, subline showing the matched field, 50-row cap with "N more", collapsed-container annotation, hits hidden by filters rendered as disabled + eye-slash and not locatable, max height about 40% of the canvas with internal scrolling); Verify: component tests cover each rule above one by one
- [x] 12.3 Implement miss fade (only toggles a style class, removes no elements, triggers no layout, takes no part in the visibility computation; mutually exclusive with focus fade; zero hits fades the whole graph); Verify: component tests assert the mutual exclusion and the zero-hit case
- [x] 12.4 Implement locate (expand the collapsed ancestor chain → select and open detail when eligible → fit to the closed neighborhood → clear the query → close the list); Verify: e2e asserts that after locating a node inside a collapsed container the node is visible, selected, and the search box is cleared
- [x] 12.5 Implement keyboard interaction inside the input (↑↓ skip disabled rows and scroll-follow, Enter locates or fits immediately, two-stage Esc, events do not bubble to the app shell); Verify: component tests cover the four key behaviors one by one

## 13. Node detail

- [x] 13.1 Rewrite the node detail panel frame and open / close behavior with Tailwind (closing does not clear the selection, clicking an already selected node again reopens it, decorative containers do not open it); Verify: component tests cover the three open / close situations
- [x] 13.2 Implement the Alerts section and alert table (one row per alert, hovering the Count column lists every occurrence time, Severity badge, missing cells show a muted "n/a", the whole section does not render when there are no alerts); Verify: component tests cover each rule one by one
- [x] 13.3 Implement the "Last occurred" column: clicking sets the view time range to `[t-300, t+300]`; Verify: a component test asserts the view time range changes to that window after clicking
- [x] 13.4 Implement the Application and Containers change-report sections (gated on `data.application` / `data.containers`; links open with `target="_blank" rel="noopener noreferrer"`, not `window.open`); Verify: component tests cover the gating conditions and link attributes
- [x] 13.5 Wire up change-history fetching (`endpoints.codeChanges` / `endpoints.configChanges`; when not configured the whole section does not render and no request is issued); Verify: a component test asserts no request is issued and the section is absent when the endpoint is not configured
- [x] 13.6 Implement the Dashboard button: applicability check (excludes cluster / storage-cluster / namespace / application), parameter assembly (including `from_time` / `to_time`), available only on a 200 with at least one non-empty url, a single link renders as a link button, multiple links as a Radix dropdown menu; Verify: component tests cover applicability, both single- / multi-link presentations, and that non-applicable nodes issue no query

## 14. Sankey view

- [x] 14.1 Implement the pure function deriving Sankey nodes and links from the normalized graph (tier chain `pod → pvc → netapp-aggr → netapp-node`; pod→pvc from `pod-mounts-pvc`, pvc→aggr from `pvc-to-netapp-aggr`, aggr→netapp-node from the compound `parent`; an edge whose endpoint kinds do not match is ignored); Verify: unit tests assert the derived node and link sets against the fixture
- [x] 14.2 Implement weight propagation (pvc→aggr is the measured value, aggr→netapp-node is the same-direction sum of its incoming edges, pod→pvc is the measured value split evenly among the pods mounting that pvc); Verify: unit tests cover the three propagations and the multi-pod even split
- [x] 14.3 Implement the missing-value rules (no measurement in a direction means no link drawn in that direction, neither direction means the whole link is excluded, `0` is drawn as a minimum-thickness dashed line and not treated as missing, a node whose links are all excluded is not drawn); Verify: unit tests cover each rule one by one, including that a pvc in the fixture with no storage edge does not appear
- [x] 14.4 Implement in-tier ordering (descending by total flow in the current mode, ties by label) and bytes/sec formatting (SI base 1000, 3 significant digits, small non-zero values shown in exponent form rather than truncated to 0); Verify: unit tests cover ordering determinism and formatting boundary values
- [x] 14.5 Compute the layout with d3-sankey and render SVG with React (the layout is changed in 14.10 to a self-computed pure function and that dependency removed) (nodes, bidirectional links, labels), colors from `tokens.ts`, read / write distinguished by more than hue; Verify: component tests assert that in Both mode the same source/target pair produces two distinguishable links, and rendering is correct in both dark and light
- [x] 14.6 Implement the Read / Write / Both mode selector (default Both) and hover full-path highlighting (highlights every upstream and downstream link passing through that node, restored on leave); Verify: component tests cover the three modes and the hover highlight set
- [x] 14.7 Implement node and link tooltips (node total in / out flow, `usage` for pvc / aggr, `health` for aggr / netapp-node; link source, target, direction, value, pvc→aggr additionally shows `maxBytesPerSec` / `maxIops` as information only with no coloring or warning; pod→pvc marked as split evenly with the pod count noted); Verify: component tests cover each rule one by one
- [x] 14.8 Implement the two empty state variants (no bytes/sec measurement anywhere in the graph; and measurements present but none in the current direction) and the cluster selector (appears only when the K8s cluster count is ≥ 2, scope covers the pod and pvc tiers); Verify: component tests assert the two empty state texts differ, and that selecting the `dr` cluster in the fixture is empty
- [x] 14.9 Implement cross-view locate (clicking a node navigates to the Graph view and locates that node; when the target is hidden by filters show a notice, do not change filters, do not select) plus size responsiveness and in-place update on refresh; Verify: e2e asserts that after clicking a Sankey node the app lands on the Graph view with that node selected

- [x] 14.10 Rewrite the Sankey layout as a self-computed pure function (column x by tier, deterministic ordering within a column, slot stacking with minimum row height and fixed spacing, cubic Bézier ribbon geometry and a single thickness scale shared across the whole graph), and remove the `d3-sankey` dependency from `package.json`; Verify: unit tests assert the four-step layout produces identical output for identical input, `npm ls d3-sankey` finds no such package and `npm run build` succeeds
- [x] 14.11 Implement box card node rendering (title row / divider / subtitle row; pod shows `namespace`, pvc and netapp-aggr show used / capacity when both `usage` fields exist; netapp-aggr and netapp-node have dashed strokes, pod and pvc solid strokes; netapp-node is a leaf card; text inside the card is `pointer-events: none`); Verify: component tests cover the three subtitle cases and that missing usage is not filled with zero, and assert card text does not intercept hover on the ribbons beneath it
- [x] 14.12 Implement slot ordering and box card sizing (incoming edges attach to the left edge, outgoing to the right; on the same side ordered by weight descending, ties by the opposite side's `label`; slot height is the larger of ribbon thickness and minimum row height; card height derives from the two stacks and the minimum body height, each stack vertically centered on its own); Verify: unit tests assert slot order and coordinates, including that a zero-weight slot still occupies the minimum row height and adjacent ribbons do not overlap
- [x] 14.13 Implement gradient ribbons and on-ribbon values (a single scale shared across the whole graph, recomputed after switching mode or cluster, cubic Bézier filled area, linear gradient in the direction's color family, value label with a stroke halo, label omitted when ribbon thickness is less than the text height); Verify: component tests assert read and write share the same scale in Both mode, the maximum thickness moves to the new maximum weight after switching mode, and zero-weight links carry no value label
- [x] 14.14 Implement column headers and namespace grouping of the pod tier (four column headers, no header drawn for an empty tier; same-namespace pods adjacent, left-edge color bar, palette colors taken in order of first appearance and cycled, pods without a namespace carry no color bar and sort last); Verify: component tests assert grouping order determinism and color bar colors, and assert namespace is never drawn as a node or link
- [x] 14.15 Implement the two numeric summary tables outside the graph (node summary with tier, `label`, total inflow, total outflow, usage, health, missing values shown as placeholders; namespace subtotals descending by total; not drawn during empty states; horizontal scrolling inside its own container when too wide); Verify: component tests cover updating with mode and not filling missing values with zero, e2e asserts the page shows no horizontal scrollbar
- [x] 14.16 Switch to `viewBox` meet-fit and implement in-graph zoom / pan (a single `<g transform>`, `<defs>` kept outside that `<g>`, pointer-anchored wheel zoom with `preventDefault`, drag pan with grab / grabbing cursors, zoom factor bounds, no tooltip while panning, opening as "fit to window but never enlarged beyond 1:1", viewport preserved across mode, cluster and theme switches and container resize); Verify: unit tests assert the graph coordinate under the anchor is unchanged before and after zooming, component tests assert a container resize does not call the layout function
- [x] 14.17 Implement the zoom control bar and keyboard operation in the graph area (zoom out / zoom factor readout that returns to 1:1 when triggered / zoom in / fit to window / 1:1 / focus, all with accessible names and keyboard operable; `+` `-` `0` `1` `F` `Esc` act only while the graph area container or a descendant has focus, listeners registered on the container rather than `document`); Verify: component tests assert pressing `0` with focus in the nav bar does not change the viewport, and these keys do not intercept the mode / cluster selectors
- [x] 14.18 Implement focus mode (hides the app shell nav bar and Sankey's own controls, the graph fills the viewport, `Esc` or triggering the control bar button again exits, viewport, mode and cluster preserved on entry and exit, not in the URL and not persisted, released on switching away from the view); Verify: e2e asserts the nav bar is not visible after entering, restored after exiting, and the zoom factor is unchanged
- [x] 14.19 Tooltip now follows the pointer, is clamped inside the view area, and does not open while a pan is in progress; Verify: component tests assert hovering the bottom-right-most node does not overflow the tooltip out of the viewport and the page shows no scrollbar, and no tooltip appears during a pan
- [x] 14.20 Add theme and performance regressions (box cards, column headers, ribbon gradients, value halos, color bars, control bar and focus mode background all taken from tokens; zoom / pan only updates `transform`); Verify: component tests assert no hard-coded colors in dark and light, a performance test with a synthetic graph asserts 100 zoom / pan operations call the layout function 0 times and each update completes within one animation frame

## 15. Container and Kubernetes deployment

- [x] 15.1 Write the multi-stage `Dockerfile` (build stage runs typecheck + build; runtime stage contains only `dist/` and a static web server, numeric non-root UID, listens on `8080`); Verify: `docker build` succeeds, `curl localhost:8080` after `docker run` returns `index.html`
- [x] 15.2 Configure the web server: history-API fallback (unknown paths return `index.html`), long-lived immutable caching for `/assets/*`, `index.html` `no-cache`, `/srv/config/config.json` served at `<base>/config.json` with `no-store`, `GET /healthz` returns 200; Verify: `curl` each path one by one checking the status code and `Cache-Control` header
- [x] 15.3 Implement the optional same-origin reverse proxy (`/api/` → backend, given by `KSG_API_PROXY_TARGET`; when not set, `/api/*` returns 404 rather than `index.html`); Verify: with it unset `curl /api/x` gets 404; once set, the request is forwarded
- [x] 15.4 Write the `deploy/` manifests (Deployment with liveness / readiness probes and resource requests, Service, an example ConfigMap carrying the complete `config.json`; config mounted as a directory at `/srv/config`, not `subPath`; compliant with the `restricted` Pod Security Standard); Verify: after `kubectl apply -f deploy/` the Pod becomes Ready, and `kubectl port-forward` can open the page
- [x] 15.5 Validate the "same image + different ConfigMap" deployment premise: with a ConfigMap setting `demoMode: true` and no reachable backend, the image still serves the demo normally; Verify: deployed with that ConfigMap, the page renders the complete graph and shows the demo badge

## 16. E2E and CI

- [x] 16.1 Write the demo mode smoke e2e: load `/graph` rendering the complete graph, switch to `/sankey` and back, collapse and restore the legend; Verify: `npm run e2e` passes
- [x] 16.2 Write the fetch path round-trip e2e: walk the full fetch, validation and rendering path with `demoMode: false` + `endpoints.graph: "/demo/graph.json"`; Verify: `npm run e2e` passes
- [x] 16.3 Create the CI workflow: Node version taken from `.nvmrc`, chain is `typecheck → lint → fixture:check → unit → e2e → build`; Verify: CI is green on the PR, and deliberately breaking any link makes the corresponding step fail
- [x] 16.4 Create the image build and push workflow (triggered on push to main and on tags, tagged `main` / `sha-<short>` / `X.Y.Z` / `latest`, using only platform-provided credentials); Verify: after one push to main the registry shows the image with the corresponding tags
- [x] 16.5 Write the README: Quick Start (`npm install` → `npm run dev`, no Docker needed), connecting a real backend, build and deployment, troubleshooting (CORS, stale `public/demo/graph.json`, configuration errors from unconfigured endpoints); Verify: following the README step by step on a clean checkout brings up both demo mode and connected-backend mode

## 18. Spec revision regressions (orphan cascade, legend, resize, collapse gesture and signals)

- [x] 18.1 Add a **baseline** input to the visibility computation (the set of node ids that have an incident edge in the normalize boundary output), and change it to: a leaf is kept or dropped by whether it has an edge in the baseline (yes → always kept, no → hidden), containers keep the fixed-point cascade of "removed when no visible child node and no visible incident edge"; container / leaf is determined by the parent-child relationship in `elements`, not by the current visibility of child nodes; Verify: unit tests cover a leaf with baseline edges but all currently filtered out being kept, a leaf with no baseline edges being hidden, both together each getting their result, a container whose children are all filtered out being collapsed away, and a container not being misjudged as a leaf
- [x] 18.2 Callers supply the baseline to the visibility computation (the baseline is taken from the normalized output, before the pod-parent mode transform and every filter); Verify: a component test asserts that in `node` mode a pod whose only edge is `pod-to-node` (DaemonSet / Job / CronJob) stays visible
- [x] 18.3 The `EdgeLegend` folded row now renders by group existence: if any member type of a folded group exists in the data, the representative row renders (label and glyph do not change because the anchor type is absent), with a toggle button; Verify: component tests assert that with only `pod-calls-service` / `service-selects-pod` and no `pod-calls-pod` the `pod ↔ pod/service` row still renders and can be toggled, and likewise for `node-to-switch` without `switch-to-switch`
- [x] 18.4 Container size responsiveness splits resize from fit: every size change calls `resize()`, only those caused by a **window size change** call `fit()`; in-app layout changes (legend collapse / restore, view switched back to visible) only resize; Verify: component tests assert zoom / pan unchanged after collapsing the legend, and fit only after the window is resized
- [x] 18.5 `useNodeDashboardUrl` now uses the configured endpoint URL as-is (removing trim and trailing-slash stripping), per runtime-config's "URL values MUST be used as-is"; Verify: a unit test asserts an endpoint with a trailing slash is sent as-is
- [x] 18.6 The double-click collapse gesture extends to `isStorageCluster`; Verify: unit tests assert double-clicking a storage-cluster toggles its collapsed state, and double-clicking a selectable container remains a no-op
- [x] 18.7 Selection mirroring and fade application take the collapse set as an explicit input (no longer using `elements` identity as a proxy); Verify: a component test asserts that when the `elements` reference is unchanged but the collapse set changes, the selection ring and fade class are reapplied

## 19. Code review regressions (query string, Sankey cluster, viewport, locate, hover, base URL)

- [x] 19.1 `withQuery` now **replaces** a repeated key rather than appending (other parameters carried by the configured URL are kept as-is, not re-encoded); Verify: unit tests assert an endpoint with `?start=100` ends up with only one `start` after this request's window is added, and `tenant=ops` and `%20` encoding are kept as-is
- [x] 19.2 Sankey's cluster filter moves ahead of aggregation (filtering pvc→aggr and pod→pvc edges rather than the finished graph): the storage tier is not filtered by cluster, aggr→netapp-node weights sum only in-scope pvc edges, aggr / netapp-node with no in-scope edge are not drawn; Verify: unit tests use a synthetic graph with two clusters sharing one netapp-node to assert weight recomputation and the disappearance of ghost nodes
- [x] 19.3 Remove the synthetic `window resize` dispatched when the Graph view switches back to visible (the container ResizeObserver is sufficient to re-measure), and remove the now-useless `visible` prop; Verify: a component test asserts GraphView dispatches no window resize event on becoming visible again
- [x] 19.4 Cross-view locate clears the search input before executing (locate from the in-view result list is cleared by SearchBar itself, not duplicated); Verify: a component test asserts the search input is empty after `locateNodeId` arrives
- [x] 19.5 Sankey clears the hover highlight and tooltip when a refresh removes the hovered node (if the node remains, recompute against the new topology); Verify: component tests assert the tooltip closes and no residual fade remains after the target node disappears, and one that still exists keeps its highlight
- [x] 19.6 `BrowserRouter` uses `import.meta.env.BASE_URL` as `basename`, consistent with the base runtime-config reads `config.json` from; Verify: component tests with `BASE_URL=/ksg/` assert `/ksg/sankey` shows Sankey and `/ksg/` redirects to `/ksg/graph`
- [x] 19.7 Revise the specs to match decisions already implemented: app-shell's view time range becomes the time window of the graph query, graph-data-source gains a "query string of the graph request" requirement, a new `graph-filters` capability spec is added, the runtime-config endpoint table gains `labelValues` / `edgeTypes`, dev-environment puts e2e into the CI gate; Verify: `openspec validate init-pure-ui-frontend --strict` passes, and no residual "graph queries carry no time parameters" statement remains anywhere in the repository

## 20. Splitting the two endpoints (graph and storage-graph)

- [x] 20.1 `runtime-config`: add `endpoints.storageGraph` to the type and validator (optional, URL-form rules same as the other endpoints), and confirm validation and error messages for `labelValues` / `edgeTypes` are complete; Verify: unit tests cover absent, empty string treated as absent, non-http(s) scheme, non-string, and that an absent `storageGraph` does **not** constitute a configuration error
- [x] 20.2 Wire types and normalize: add the `netapp-svm` node kind, the `storage-flow` edge type (with the five `labels.tier` values and the optional `labels.attribution`), and the `hardware` / `perf` attributes of `netapp-node`; Verify: unit tests assert all five tiers map correctly, `attribution` is kept as-is, `hardware` / `perf` degrade field by field with no value filled in when absent, and `perf` does not affect `health` or the status outline
- [x] 20.3 `buildStorageGraphRequestUrl` and its refetch key: `start` / `end` (resolved at send time), single-valued `az` / `env`, repeatable `ontap_cluster` / `node` / `aggr` / `svm` / `pod`, optional `cluster` / `namespace`, no `edge_type` / `prune` sent; `pod` values validated as `<ns>/<name>` before sending; Verify: unit tests cover parameter combinations, URL encoding, that the clock advancing does not change the refetch key, and that a missing az or env produces no request
- [x] 20.4 The shell adds a **second** loader instance serving storage-graph: lazy (the first request is issued only on first entering Sankey with `az` / `env` both present), independent in-flight / status / error / last-success time; Verify: component tests assert the storage-graph request count is 0 while staying on the Graph view, exactly one after first entry, no more when switching back and forth, and a 500 on one side does not affect the other
- [x] 20.5 The reload action and auto-refresh now act only on the **current view's** source, and the status indicator reflects the current source; reload is shown as unavailable when `az` / `env` are not both selected or the endpoint is not configured; Verify: component tests assert triggering reload on Sankey hits only storage-graph, and after switching views the indicator immediately shows the new source's time and error
- [x] 20.6 The view time range now drives both endpoints: a change refetches only sources **already loaded**; Verify: component tests assert changing the time range before Sankey has ever been opened produces no storage-graph request, and when both are loaded each is refetched once with identical `start` / `end`
- [x] 20.7 Sankey estate selector: `az` / `env` single-select, options from `endpoints.labelValues`, auto-preselect when there is exactly one candidate value, show a notice and do not fetch when not both selected, clear the selection when its option disappears; Verify: component tests cover the four situations, and assert no interaction with the same-named dimensions in the Graph filter bar
- [x] 20.8 Sankey root controls: the five root kinds are repeatable and mixable, `pod` values validated inline, the control states explicitly that `node` matches both node kinds and takes the intersection of both sides, empty is a valid state; Verify: component tests assert an invalid pod root does not enter the request while other roots still work, and a root change triggers exactly one refetch
- [x] 20.9 Sankey's optional `cluster` / `namespace` narrowing controls (multi-select, sent as request parameters, not filtered client-side); Verify: a component test asserts a selection changes the request parameters rather than the elements already returned
- [x] 20.10 Rewrite the Sankey derivation: six tiers, tier membership read from `labels.tier`, FlexGroup paths start at `svm-pvc`, unscheduled pods end at `pvc-pod`, no-flow roots are still drawn as orphan nodes; Verify: unit tests use the storage fixture to assert the members of the six tiers, and cover the three incomplete paths
- [x] 20.11 Weights now come directly from the `data.metrics` of `storage-flow` edges; **remove** the client-side aggregate summing and pod even-split logic and their tests; `labels.attribution="split"` is marked in the tooltip as a split estimate; Verify: unit tests assert an upstream weight is not replaced by the sum of downstream (including inequality caused by backend rounding), a split link is marked as an estimate while an `svm-pvc` link is not
- [x] 20.12 Remove the client-side cluster selector and its filtering logic (achieved through request parameters instead), together with their tests; Verify: `rg` confirms no residual client-side cluster filtering code, existing tests all green after updating
- [x] 20.13 Empty state becomes four states (endpoint not configured / estate not both selected / empty response / no measurement in the current direction), each with distinguishable text, the third additionally marking the fixture source in demo mode; Verify: component tests assert the text per state and that the selectors remain operable
- [x] 20.14 Tooltip and ordering updates: node tooltip gains `ontap_cluster`, `hardware`, `perf` (marked as raw readings and not colored), `alerts` and a no-flow root explanation; link tooltip shows the tier, ceiling / latency only on `svm-pvc`; ordering places no-flow roots at the bottom of the tier; Verify: unit and component tests assert each rule one by one
- [x] 20.15 Cross-view Locate updates: the two notices for a target hidden by filters and a target absent from the graph's current body (different filters or `prune`) are distinguishable, `netapp-svm` nodes offer no Locate interaction; Verify: component tests assert the three situations, and that none of them rewrites the user's filters or `prune`
- [x] 20.16 Add the `SHOWCASE_STORAGE_GRAPH` fixture (all five tiers present, including one `pvc-pod` with `attribution="split"`, one FlexGroup path, `hardware` / `perf`), `fixture:build` now produces two JSON files, `fixture:check` covers both; Verify: unit tests assert the storage fixture's weights are conserved per tier, and that its pod / pvc / netapp node ids all exist in the graph fixture (except SVM)
- [x] 20.17 Deployment and docs: the example ConfigMap and `deploy/README.md` gain `endpoints.storageGraph` (and `labelValues` / `edgeTypes`), cache headers cover `/demo/storage-graph.json`, README troubleshooting gains "Sankey shows endpoint not configured" and "Sankey requires one az / env each"; Verify: after applying on a kind cluster per the README, Sankey draws a graph
- [x] 20.18 Add a third e2e spec: serve both demo payloads at once, assert the storage-graph request count is 0 while staying on Graph, exactly one after entering Sankey and selecting `az` / `env`, and the drawn tiers come from the storage fixture; Verify: `npm run e2e` passes all three specs

## 21. Fixes after code review

- [x] 21.1 Unify the bytes ladder on `KB`: delete `formatSiBytes` inside `deriveSankey`, which is verbatim identical to `shared/format/measurements.formatBytes`, and make `formatBytesPerSec` delegate; Verify: a unit test asserts `262144` → `262 KB/s`, and the rate and `usage` within the same tooltip use the same spelling
- [x] 21.2 Sankey `az` / `env` still render and accept input when `endpoints.labelValues` is absent (degrading to free text), while `cluster` / `namespace` do not render when there are no enumerable options; Verify: component tests assert `AZ` / `Env` are inputs and accept typing with zero options, shell tests assert that with only `storageGraph` configured a request carrying `az` / `env` can still be sent and no label-values URL is hit
- [x] 21.3 The image gains a second proxy `KSG_METRICS_PROXY_TARGET` → `/metrics-api/`, and the example ConfigMap's `endpoints.labelValues` changes to `/metrics-api`; Verify: comments in `deploy/README.md` and `deployment.yaml` explain the two are different upstreams, and `/metrics-api/` returns 404 when not set
- [x] 21.4 Roots on a path with no measurement are still drawn: `deriveSankey` accepts the request's root selection and keeps nodes by the same matching rules as the backend (`node` matches both sides, `ontap_cluster` covers the three kinds under it, `pod` is `<ns>/<pod>`, `pvc` is not a root kind); Verify: unit tests assert that on a path with no `metrics` at all only the nodes selected as roots are kept with `noFlow`, and no node is kept when roots are all empty
- [x] 21.5 No-flow node stacks are kept within the view: the flow graph reserves height for the tallest stack, and a stack compresses its spacing when space runs short; Verify: a component test with 20 no-flow nodes in a single tier asserts every node's bottom edge stays within the SVG height

## 22. Connecting the alert overlay to the real backend

The alert overlay of `kube-state-graph` sends `{name, state, severity}` without any occurrence time; yet normalize's `parseAlerts` requires every alert to carry a valid occurrence time, otherwise it is discarded. The result is that the real backend's alerts **all** vanish at the anti-corruption boundary — the API body carries `data.alerts`, the UI shows not a single one, and the response is a 200 with no error to follow.

- [x] 22.1 Correct the wire contract: the "PANEL-ONLY, the backend does not send alerts" note on `WireAlert` no longer holds (the backend sends them since `e6430ad`); replace it with a record of the two producers' differing understanding of "time", and declare the optional `state` field as **deliberately not projected** (the query carries a fixed `alertstate="firing"` selector, so the presentation would only ever be a constant); Verify: the types compile, the comment names the fields each of the two producers sends
- [x] 22.2 `NodeAlert.timeRecords` becomes optional, and the type comment states that "missing means no occurrence history" is a complete state rather than a defect; Verify: `npm run typecheck` passes, every consumer is exposed one by one by the compiler
- [x] 22.3 `parseAlerts` no longer discards an alert for lacking an occurrence time: `name` / `severity` are the only two required fields, and with no valid time `timeRecords` is **omitted** (must not be written as `[]`); Verify: unit tests assert an overlay-shaped alert is kept without `timeRecords` (using `toStrictEqual` to distinguish omission from an empty array), `state` is not projected, and one missing `name` / `severity` is still discarded
- [x] 22.4 Alert table degradation: when `timeRecords` is missing, Count and Last occurred each show a muted "n/a", Last occurred is no longer a button (there is no instant to rewind to), and `0` and the epoch start date must not stand in; Verify: component tests assert the row still renders, both cells are n/a, no `alert-count` / `alert-time` testid, no button, and rows with times in the same table are unaffected
- [x] 22.5 `ontap-prod-02` in `SHOWCASE_STORAGE_GRAPH` carries both one alert with `time` and one overlay-shaped alert, so demo mode walks both the normal and the degraded path; Verify: `npm run fixture:check` passes, fixture tests all green

## 23. `severity` is optional too

The cross-repo spec walkthrough found the same class of defect one field short: the backend's `AlertDTO.Severity` is serialized with `omitempty` (its `graph-api` spec states explicitly that "`severity` is omitted from an entry when empty"), while `parseAlerts` requires a non-empty `severity`, otherwise the whole entry is discarded. Alerting rules that declare no severity label therefore vanish silently in the same way — the same failure shape as §22's `timeRecords`, differing by one field.

- [x] 23.1 `WireAlert.severity` and `NodeAlert.severity` become optional, with type comments stating the backend serializes with `omitempty`, and that "missing" and "an unrecognized custom label" are two different things; Verify: `npm run typecheck` passes, every consumer is exposed by the compiler
- [x] 23.2 `parseAlerts` requires only `name`: when `severity` is missing / an empty string / not a string, **omit the field** and keep the alert, never substituting a default grade; Verify: unit tests use `toStrictEqual` to assert all three inputs produce an alert without a `severity` field, assert `{name:'Ungraded', state:'firing'}` produces `{name:'Ungraded'}`, and one missing `name` is still discarded
- [x] 23.3 The alert table's Severity column shows a muted "n/a" rather than a badge (including the fallback color) when missing, while custom labels still get a badge as usual; Verify: component tests assert no `alert-severity` testid, the custom label `P1` still gets a badge, and an alert carrying only `name` renders as one row of five n/a with Last occurred not a button
- [x] 23.4 The alert line in the Sankey node tooltip prints only the name when `severity` is missing, never an `undefined` prefix; Verify: type check passes, the fixture walks that branch
- [x] 23.5 `aggr1` in `SHOWCASE_STORAGE_GRAPH` carries one alert without `severity`, so the fixture covers all four combinations of "graded or not × occurrence history or not"; Verify: `npm run fixture:check` passes

## 24. Grafana-style dropdowns, page split and URL scope

Supersedes the keep-mounted semantics of 7.6, the reveal clause of 18.4 and the shell-held second loader of 20.4–20.6: pages own their loaders, switching resets, the URL carries scope.

- [x] 24.1 Add `@radix-ui/react-popover`; build the shared dropdown (single / multi, search filter, pinned `All` row, custom-value row gated by `allowCustom`, pills with `×` and `+N` overflow, unlisted marker, ARIA combobox / listbox roles, `↑` `↓` `Enter` `Esc` `Tab`, focus return); Verify: component tests cover every scenario of `graph-filters` "Interaction contract of the dropdown control"
- [x] 24.2 Rewrite `FilterBar` on the shared dropdown: identity dimensions allow custom values, `edge_type` does not, Projection single-select; remove the chip row and the native `<select multiple>`; Verify: FilterBar tests updated plus custom-value / edge-type-no-custom / pill-overflow scenarios
- [x] 24.3 Rewrite `SankeyScopeBar` on the shared dropdown: `az` / `env` single-select with custom values (replaces the free-text fallback), `cluster` / `namespace` multi-select, root kind single-select; Verify: component tests assert zero-option `az` / `env` still accept a typed value and unlisted values render with the marker
- [x] 24.4 URL scope codec: pure `parse` / `serialize` per page (Graph: `cluster` / `az` / `env` / `namespace` / `edge_type` / `prune`; Sankey: `az` / `env` / `ontap_cluster` / `node` / `aggr` / `svm` / `pod` / `cluster` / `namespace` / `mode`) plus shared `from` / `to`; defaults omitted (time always written), canonical order, unknown params stripped, `prune` / `mode` validated, invalid `pod` roots dropped with an inline notice, invalid `from` / `to` ignored as a pair; Verify: unit tests round-trip every scenario URL in app-shell / graph-filters / storage-flow-sankey
- [x] 24.5 `useUrlScope` hook over `useSearchParams`, writing with `replace: true` only when the canonical string changes; `useViewTimeRange` gains URL-first resolution and write-back on mount; Verify: component tests assert history length unchanged across in-page changes and a bare path rewritten to carry `from` / `to`
- [x] 24.6 Restructure routing: layout route (NavBar + `<Outlet>`) with `GraphPage` and `SankeyPage`; pages own loader, control bar and view state; NavBar reads the current page's status through a small context, and Sankey focus mode hides nav chrome through the same context; delete `ViewHost`, the `hidden` containers, `graphMounted` / `sankeyMounted` / `sankeyVisited` and the `visible` prop plumbing; Verify: component tests assert the non-current page is absent from the DOM and that switching re-fetches
- [x] 24.7 Loader: accept an `AbortSignal`, abort in flight and clear the refresh timer on unmount, discard late results; Verify: unit tests assert no state update after unmount and no timer leak
- [x] 24.8 Locate via navigation state: Sankey calls `navigate('/graph', { state: { locate: id } })`; `GraphPage` consumes it after the first successful load (retained through a failed first load) then clears it; refresh does not repeat; Verify: component tests cover found / not-in-body / svm-not-clickable and refresh-does-not-repeat
- [x] 24.9 Remove dead code and update tests for reset semantics: resize-on-reveal path in `GraphCanvas`, in-memory-only `useGraphFilters`, cross-view state-preservation tests, `visible`-driven Sankey initial-viewport logic; Verify: `rg` shows no remaining view-visibility plumbing; `npm run test:ci` green
- [x] 24.10 e2e: (a) `/graph?namespace=shop&from=now-1h&to=now` populates the dropdown and the request; (b) Sankey → Locate → Back restores `/sankey?az=…&env=…&aggr=…`; (c) refresh on `/sankey?…&mode=write` keeps scope and mode; (d) dropdown keyboard flow (open, search, toggle, `Esc` focus return); Verify: `npm run e2e` green
- [x] 24.11 Docs: README gains a "URL parameters" table per page (incl. `from` / `to`), states that switching views resets and that links are snapshots; walkthrough of app-shell / graph-filters / storage-flow-sankey / graph-data-source / graph-view against the revised requirements; Verify: walkthrough maps each revised requirement to a test or observable behaviour

## 25. Fixes after the §24 verification pass

The verification pass found the implementation right and two specs stale behind it, plus three
requirements whose branches no test reached.

- [x] 25.1 `runtime-config`'s "absent optional endpoints" mapping is revised to match `graph-filters` and the code: an absent `labelValues` no longer removes the Graph filter bar's `cluster` / `az` / `env` / `namespace` controls — they render with an empty option list and still take a custom value, because those dimensions reach the upstream PromQL as raw label matchers; an absent `edgeTypes` leaves `edge_type` rendered with no options and no custom value; only the Sankey's `cluster` / `namespace` still disappear. The two scenarios stating the old behaviour move with it; Verify: `openspec validate init-pure-ui-frontend --strict` passes and no spec still says an identity control does not render
- [x] 25.2 `storage-flow-sankey` states the demo-mode exemption its own rules never carried: with `demoMode` true the page reads and writes none of `az` / `env` / the five root kinds / `cluster` / `namespace` / `mode` in the query (`from` / `to` still written, they belong to `app-shell`), holding them in component state as the Graph page already does; Verify: a shell test opens `/sankey?az=…&env=…&mode=write&aggr=…` in demo mode and asserts the query keeps only `from` / `to`, and that no fetch is issued
- [x] 25.3 Locate's rejection branches gain the component tests §24.8 claimed: `locate-missing` for a target absent from the graph body, `locate-filter-hidden` for one the kind filter is hiding (driven through `service`, which the legend draws an eye for — pods sit inside collapsed containers and get no toggle row), and **no** notice while `hasPayload` is false, so a failed or still-flying first load keeps the target instead of reporting it absent from a result that does not exist; Verify: `GraphView.test.tsx` asserts each testid, its `role="status"`, that the two notices are mutually exclusive, and that Dismiss removes one
- [x] 25.4 The shell's locate test asserts the one-off requirement it only named: after the view consumes the target the page drops it, a following query write does not re-arm it, and a remount at the same URL locates nothing; Verify: `AppShell.test.tsx` consumes through the mocked view, changes a filter, and remounts
- [x] 25.6 The shared dropdown stops drawing the `All` row when there is nothing for it to empty: with zero options AND no selection a custom-value multi-select shows the search input and (once something is typed) the custom-value row, and no more, as `graph-filters` "No options" requires; a value added afterwards unions into the list and brings `All` back, so the dimension is never a one-way door; Verify: `ScopeSelect.test.tsx` asserts the empty popover has no `option` role at all, and that `All` returns and clears once a custom value is the only entry
- [x] 25.5 `deploy/README.md` and the walkthrough follow: az / env are custom-value dropdowns, not free-text fields; the walkthrough gains rows for the locate rejections, the demo-mode exemption and the still-rendering filter bar, and names `useViewTimeRange.test.tsx` by its real extension; Verify: `rg 'free-text'` finds no claim about az / env, and every walkthrough row names a file that exists

## 26. The Sankey drew at fit squared

Running the demo estate through the front door showed the Sankey at 13% jammed into the
top-left of its area while the control bar read 36%. Two specs required two different
mappings and the implementation honoured both: "Sizing and container resize" made the SVG's
`viewBox` the layout's intrinsic size with `preserveAspectRatio` meet-fit, while "Zoom and
pan of the chart area" made the `<g>` transform a viewport `useZoomPan` computes entirely in
CSS pixels (fit centres against the ResizeObserver's measurement, the wheel anchors on
`clientX - rect.left`, a drag adds raw client deltas, `percent` reports `scale * 100` as a
1:1 level). The two mappings compose, so every one of those numbers was wrong by the viewBox
factor — `fit` squared its own scale, pans moved short, and wheel zoom drifted off the
pointer. Only the scale was visible; the rest read as "the diagram feels sluggish". The
resolution is the pixel-space one, because it is the contract the zoom / pan requirements
already state and the only one under which the readout can mean 1:1.

- [x] 26.1 `SankeyChart`'s SVG drops `viewBox` and `preserveAspectRatio`, with a comment naming the double mapping so neither comes back; Verify: `npm run typecheck` and `npm run lint` pass, and the chart draws at the percentage the control bar reports
- [x] 26.2 `storage-flow-sankey` "Sizing and container resize" requires **no** `viewBox` (one user unit is one CSS pixel, the transform is the only scaling), and its resize scenario asserts a preserved viewport instead of a proportional re-fit; the "all nodes inside the intrinsic frame" rule keeps its force, now anchored on "fit to window" rather than the viewBox; Verify: `openspec validate init-pure-ui-frontend --strict` passes and no spec still requires a `viewBox`
- [x] 26.3 `app-shell`'s view-area requirement follows: the Sankey resizes without re-layout **and** without re-fit, so the preserved viewport is the rule rather than an exception carved out of a re-fit; Verify: the two specs no longer disagree about what a resize does to the Sankey
- [x] 26.4 `design.md` states why the absent `viewBox` is load-bearing rather than incidental; Verify: the design note names the pixel-space contract `useZoomPan` depends on
- [x] 26.5 A regression test asserts the SVG carries no `viewBox`, and that "fit to window" scales the layout to sit inside the measured container while the readout matches the drawn scale; Verify: `SankeyView.test.tsx` fails with the attribute restored and passes without it
- [x] 26.6 The opening viewport waits for a REAL container measurement instead of fitting against a seeded placeholder. Removing the viewBox exposed this second half: the box was seeded at 800x480, the opening effect runs in the same commit as the one that installs the ResizeObserver, so it fitted and locked against the placeholder before any measurement arrived — 2096-wide content opened at 38% in a 1600px window with room for 76%, which reads as "the diagram is too big for its area". The size is now `null` until measured (the box is also measured up front with `getBoundingClientRect`, so the real value lands a commit sooner than the observer's first delivery), and `UNMEASURED_CONTAINER` serves only the zoom controls in an environment that reports no layout; Verify: a test drives a ResizeObserver reporting 1600x982 and asserts the drawn scale is that container's fit and explicitly NOT the placeholder's — it fails with the seed restored, reproducing 0.38 against the expected 0.76

## 17. Overall acceptance

- [x] 17.1 Full-chain verification: on a clean checkout run `npm install && npm run typecheck && npm run lint && npm run fixture:check && npm run test:ci && npm run e2e && npm run build` with everything passing, and unit test coverage reaching 80%
- [x] 17.2 Walk through the requirements of the 13 capabilities one by one against `specs/`, confirming no omission or behavioral deviation; Verify: produce a walkthrough checklist annotating each requirement with its corresponding test or observable behavior
- [x] 17.3 Rerun the full-chain verification after the two-endpoint split (including the new third e2e spec), coverage still reaching 80%; Verify: the whole CI chain green on a clean checkout
- [x] 17.4 Re-walk the changed requirements of the six specs `runtime-config` / `graph-data-source` / `storage-flow-sankey` / `app-shell` / `dev-environment` / `container-deployment` against the revised `specs/`; Verify: the walkthrough checklist annotates each new requirement with its corresponding test or observable behavior
- [x] 17.5 Rerun the full-chain verification after the alert overlay fix; Verify: `npm run typecheck && npm run lint && npm run fixture:check && npm run test:ci` all green, and confirm against a real kube-state-graph backend that node `data.alerts` actually appear as rows in the panel
- [x] 17.6 Rerun the full-chain verification after `severity` became optional (including `npm run e2e`); Verify: the whole CI chain green, coverage still reaching 80%, and the fact that `name` is the only required field on an alert row is consistent across the `graph-data-source` and `node-detail` specs
- [x] 17.7 Full-chain re-run after §24 (`typecheck`, `lint`, `fixture:check`, `test:ci`, `e2e`, `build`); Verify: CI chain green, coverage ≥ 80%
- [x] 17.8 Full-chain re-run after §25 (`typecheck`, `lint`, `fixture:check`, `test:ci`, `e2e`, `build`); Verify: CI chain green, coverage ≥ 80%
- [x] 17.9 Full-chain re-run after §26 (`typecheck`, `lint`, `fixture:check`, `test:ci`, `e2e`, `build`); Verify: CI chain green, coverage ≥ 80%, and the Sankey confirmed against the real demo estate to draw at the percentage its control bar reports
